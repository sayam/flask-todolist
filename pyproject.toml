# config กลางของเครื่องมือคุณภาพทั้งหมด (Phase 0 — ดู docs/STANDARDS.md)
# หลักการ: เปิดทุกกฎแล้วปิดรายข้อพร้อมเหตุผล — ของที่ปิดต้อง justify ตัวเอง

[tool.pytest.ini_options]
pythonpath = ["."]
# เก็บเฉพาะเทสต์ของ repo นี้ — `vendor/` เป็น submodule ที่มีชุดเทสต์ของตัวเอง
# ซึ่ง CI ของ repo ต้นทางรันไปแล้วก่อน pin SHA นี้ (ADR 0077) · เก็บมาด้วยจะได้
# ทั้งชื่อไฟล์ชนกันและเกณฑ์ coverage ที่ไม่ใช่ของเรา
testpaths = ["tests"]
# `--strict-markers`: พิมพ์ชื่อ marker ผิดต้องแดงทันที ไม่ใช่กลายเป็น marker ใหม่
# ที่ไม่มีใครกรองด้วย (job `bare` จะรันเทสต์ที่ตั้งใจให้ข้ามโดยไม่มีใครรู้)
addopts = "--strict-markers"
markers = [
  # จุด plug ประกาศไลบรารีของตัวเองไว้ใน manifest และมันอยู่นอก `[packages]`
  # ของ core (ADR 0025) — เทสต์ที่ต้องใช้ไลบรารีพวกนั้นจึงต้องบอกตัวเองว่า
  # "ฉันไม่ใช่ของ core" job `bare` ใน CI รันด้วย `-m "not plugin_deps"`
  # เพื่อพิสูจน์ว่าที่เหลือทั้งชุดเขียวได้โดยไม่มีไลบรารีของ plugin ตัวไหนเลย
  "plugin_deps: ต้องติดตั้งไลบรารีที่ manifest ของจุด plug ประกาศไว้ก่อนถึงจะรันได้",
  # **ติดให้อัตโนมัติจาก `tests/conftest.py`** ไม่ใช่พิมพ์มือ (audit รอบ 18) —
  # โมดูลที่ไม่ import `app` และไม่ขอ fixture ที่สร้างแอปเลย คือเทสต์ชั้นกติกา
  # ที่อ่านไฟล์อย่างเดียว · job `bare` และ `dialect` ถามคำถามอื่น (ไลบรารีหาย /
  # ยี่ห้อฐานข้อมูล) จึงรันด้วย `-m "not governance"` — ประหยัดสามรอบต่อ push
  "governance: อ่านไฟล์อย่างเดียว ผลไม่ขึ้นกับยี่ห้อฐานข้อมูลหรือไลบรารีของ plugin",
]

# ---------------------------------------------------------------- ruff
[tool.ruff]
line-length = 100
target-version = "py313"
# migrations ไม่ lint: โค้ดกึ่ง generate + raw SQL เก่าที่จะถูกล้างตอน Phase 5 squash
# (กติกา dialect ของ migration คุมด้วย tests/test_migration_lint.py แทน)
# vendor/ ไม่ lint: เป็น submodule ของอีก repo ที่มี lint ของตัวเองเข้มกว่า —
# ตรวจซ้ำที่นี่คือการตั้งเกณฑ์ให้โค้ดที่เราไม่ได้เป็นคนแก้ (ADR 0077)
extend-exclude = ["migrations", "app/sun_data.py", "vendor"]

[tool.ruff.lint]
select = ["ALL"]
ignore = [
    # -- ผูกกับแผนเฟส --
    "ANN",     # type annotation: เปิดพร้อม mypy strict แบบไล่ต่อ module (Phase 2)
    # -- ขัดกับธรรมเนียมของโปรเจกต์ (มีเหตุผลบันทึกไว้) --
    "D",       # pydocstyle: docstring เป็นภาษาไทย กติกา punctuation อังกฤษใช้ไม่ได้
               # ปริมาณ docstring คุมด้วย interrogate แทน
    "DTZ",     # naive-UTC เป็นดีไซน์ที่ตั้งใจและบันทึกใน CLAUDE.md (tz.py แปลงให้)
    "RUF001", "RUF002", "RUF003",  # ข้อความไทยใช้ em-dash/curly quotes โดยตั้งใจ
    "PLC0415", # import ใน function = ท่ามาตรฐานกัน circular import ของ Flask factory
    "A003",    # model attribute ชื่อ id เป็นธรรมเนียม ORM
    "RUF012",  # Flask config-class pattern ใช้ class attribute เป็น dict ตรง ๆ
    "FBT001", "FBT002",  # boolean positional: click flags / SQLAlchemy default
    # -- ความเห็นด้าน style ที่เลือกไม่ตาม --
    "EM101", "EM102", "TRY003",  # ข้อความใน raise เขียน inline อ่านง่ายกว่าแยกตัวแปร
    "TRY300",  # return ใน try อ่านตรงไปตรงมากว่า else
    # -- ชนกับ ruff format (ตามคำแนะนำ official) --
    "COM812", "ISC001",
    "CPY001",  # ไม่ใช้ copyright header ต่อไฟล์ — license ประกาศระดับ repo

]

[tool.ruff.lint.per-file-ignores]
"tests/**" = [
    "INP001",   # tests ไม่ใช่ package (pytest หาเจอผ่าน pythonpath)
    "S101",     # assert คือหัวใจของ pytest
    "S105", "S106",  # รหัสผ่าน dummy ในเทสต์ ไม่ใช่ secret จริง
    "PLR2004",  # ตัวเลขดิบในเทสต์คือ expected value อ่านตรง ๆ ดีกว่าตั้งชื่อ
    "ARG001",   # fixture ที่ inject เพื่อ side effect (เช่น user_id) แต่ไม่ได้ใช้ค่า
    "SLF001",   # เทสต์แตะฟังก์ชันภายในได้ — นโยบายอย่าง _column_change/_hmac ต้องพิสูจน์
                # ทีละชั้น การทดสอบผ่านทางอ้อมอย่างเดียวไม่ได้บอกว่าชั้นไหนพัง
]
"scripts/generate_sun_table.py" = ["C901"]  # main() เป็นไปป์ไลน์เส้นตรงของขั้นตอน
    # generate ที่อ่านไล่บนลงล่างได้ การหั่นให้ผ่าน gate ทำให้ตามลำดับยากกว่าเดิม
"scripts/**" = ["T201", "INP001", "PLR2004", "S101"]  # สคริปต์รันมือ: print คือ output,
    # ไม่ใช่ package, ตัวเลขในสูตรดาราศาสตร์อ่านคู่กับสูตรตรง ๆ, assert เป็น sanity check
"app/plugins/*/*/*.py" = ["INP001"]  # ไฟล์ของ plugin ถูกโหลดตาม path ไม่ใช่ตามชื่อ
    # โมดูล (ดู `load_module()`) จึงไม่ต้องเป็น package — ใส่ __init__.py กลับเข้าไป
    # แปลว่าไอดีของ plugin ต้องเป็นชื่อโมดูล python ที่ถูกต้อง ซึ่งไม่ใช่สัญญาที่ตั้งไว้
"tests/**/*" = ["INP001"]
"migrations/**" = ["INP001"]

[tool.ruff.lint.mccabe]
max-complexity = 10  # ลดจาก 12 ได้หลัง Phase 3 ย้าย branch ของตัวกรองออกจาก routes (ratchet)

# ---------------------------------------------------------------- mypy
[tool.mypy]
python_version = "3.13"
# โค้ดที่ย้ายไป vendor/ ยังต้องถูก type-check ตอนที่ scripts/ เรียกมัน — บอก mypy
# ว่าหาแพ็กเกจได้ที่ไหน ส่วนตัว vendor เองไม่ถูกตรวจซ้ำ (มี mypy strict ของตัวเอง)
mypy_path = "vendor/verifiable-gates/src"
# โหมด gradual: ทั้งแอปตรวจแบบหลวม + module ที่ประกาศแล้วต้อง strict
# **ขยายได้ ห้ามหด** — และตั้งแต่ audit รอบ 14 มีตัวบังคับจริงแล้ว ไม่ใช่แค่ประโยคนี้:
# `[tool.todolist.ratchets]` ข้างล่างเก็บจำนวนโมดูลที่อยู่ในลิสต์ และ
# `scripts/check_ratchets.py` ตรวจสองทิศทุก push (หดแล้วแดง · ขยายแล้วไม่ขยับพื้นก็แดง)
# เป้าเดิมเขียนว่า "ทั้งแอปภายใน Phase 2" ซึ่ง**หมดอายุไปสิบหกเฟสแล้ว** — เป้าที่ผ่าน
# มานานขนาดนั้นไม่ใช่เป้า จึงถอนออกและใช้ ratchet แทน: ทุก PR ที่แตะโมดูลนอกลิสต์
# ควรพามันเข้าลิสต์ถ้าทำได้ ส่วนตัวที่พึ่ง extension ไร้ stub ยังอยู่นอกได้ตามเดิม
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true
no_implicit_optional = true
check_untyped_defs = true
# **ส่วนเสริมของ plugin อยู่นอกสายตาของ mypy** — ไม่ใช่การยกเว้นเพื่อความสะดวก
# แต่เพราะ mypy ตั้งชื่อโมดูลให้มันไม่ได้: ไอดีของจุด plug คือชื่อไดเรกทอรี ซึ่ง
# **มีขีดกลางได้โดยตั้งใจ** (plugin ถูกโหลดตาม path ไม่ใช่ตามชื่อโมดูล — ดู
# `load_module()` และ per-file-ignores ของ ruff ข้างบน) พอมีขีดกลาง mypy หยุด
# ไล่ package ตรงนั้นแล้วเรียกไฟล์ว่าโมดูล `provide` เฉย ๆ ผลคือสองอย่าง:
#   (ก) ไม่ตรงกับ override `app.plugins.*` ข้างล่าง ไลบรารีของส่วนเสริมจึงกลายเป็น
#       error ทั้งที่ ADR 0025 ตั้งใจให้ job นี้ไม่ต้องติดตั้งมัน
#   (ข) **ส่วนเสริมตัวที่สองทำให้ mypy ล้มทั้ง job** ด้วย duplicate module เพราะ
#       ทุกตัวใช้ชื่อไฟล์ `provide.py` ตามสัญญาเดียวกัน (ยืนยันด้วยการวางตัวที่สอง
#       ลงไปจริงแล้ว: "errors prevented further checking")
# ทางแก้ที่ mypy เสนอ ข้อ (b) ใส่ `__init__.py` ขัดกับสัญญาที่ว่าไอดีของ plugin
# ไม่ต้องเป็นชื่อโมดูล python ที่ถูกต้อง ข้อ (c) explicit-package-bases ก็ยังติด
# ขีดกลางอยู่ดี — เหลือข้อ (a) ซึ่งคือบรรทัดนี้
# **ราคาที่จ่าย:** `provide.py` ไม่ถูก type check เลย รับได้เพราะโดยนิยามมันคือ
# ไฟล์ที่ import ไลบรารีซึ่งไม่มีอยู่ในสภาพแวดล้อมของ core (mypy จึงทำอะไรได้ไม่มาก
# อยู่แล้ว) และสัญญาของมันถูกบังคับด้วย host ตอนรัน + เทสต์สแกน import ของ Phase 4.5
exclude = ["app/plugins/.*/enhancements/"]


[[tool.mypy.overrides]]
# **ใช้ type ของ vendor ได้ แต่ไม่รายงานความผิดของมัน** — โค้ดใน submodule ถูก
# mypy strict ของ repo ต้นทางตรวจไปแล้วก่อน pin SHA นี้ (ADR 0077) · ตรวจซ้ำที่นี่
# จะแดงด้วยเรื่องที่ไม่เกี่ยวกับความถูกต้อง (เช่นไม่มี stub ของ pyyaml ในสภาพแวดล้อมนี้)
# และตั้งเกณฑ์ให้โค้ดที่เราไม่ได้เป็นคนแก้
module = "verifiable_gates.*"
ignore_errors = true
follow_imports = "silent"
[[tool.mypy.overrides]]
module = [
    "flask_login.*", "flask_limiter.*", "flask_wtf.*",
    "flask_babel.*", "flask_migrate.*", "flask_sqlalchemy.*",
    "flask_talisman.*",
    "flask_smorest.*",  # marshmallow/apispec/webargs มี py.typed แต่ตัว smorest ยังไม่มี
]
ignore_missing_imports = true

# `db.Model` ของ flask_sqlalchemy เป็น attribute แบบ dynamic mypy จึงมองไม่เห็น
# ตัวคลาส — ปิดเฉพาะ error ตัวนี้ตัวเดียว ส่วนที่เหลือของไฟล์ยัง strict เต็ม
# (โค้ดแอปเลิกใช้ `Model.query` แบบเก่าแล้วตั้งแต่ Phase 3 — query ทุกจุดเป็น
# `select()` ของ SQLAlchemy 2.0 แต่ base class ยังต้องเป็น `db.Model` อยู่ดี
# เพราะ flask-sqlalchemy ผูก metadata/session ผ่านตัวนั้น)
[[tool.mypy.overrides]]
module = ["app.models", "app.audit"]
disable_error_code = ["name-defined"]

# โค้ดของ plugin import ไลบรารีที่ **ไม่ได้อยู่ในสภาพแวดล้อมของ core** ได้
# (ADR 0025 — ไลบรารีของ plugin อยู่ใน category แยก `pipenv sync --dev` ไม่ติดตั้งให้)
# ถ้าไม่ปิดข้อนี้ job `lint` จะบังคับให้ต้องติดตั้งไลบรารีของ plugin ทุกตัวก่อน
# type check ได้ ซึ่งเท่ากับยกเลิกการแยก dependency ที่เพิ่งทำไปทั้งหมด
#
# **ราคาที่จ่าย:** ชื่อโมดูลที่พิมพ์ผิดในโค้ด plugin จะไม่ถูก mypy จับ — รับได้
# เพราะเส้นทางนั้นถูกเทสต์จริงเรียกอยู่แล้ว และมีเทสต์สแกน import เทียบกับ
# manifest อีกชั้น (Phase 4.5 step ถัดไป)
[[tool.mypy.overrides]]
module = ["app.plugins.*"]
disable_error_code = ["import-not-found", "import-untyped"]

# strict list — module ที่ pure (ไม่พึ่ง extension ที่ไม่มี type stub)
[[tool.mypy.overrides]]
module = [
    "app.tz", "app.filters", "app.plugins", "app.sun_data",
    "app.models", "app.soft_delete", "app.purge", "app.audit",
    "app.i18n", "app.theme",
    "app.services.*",
    "app.api.*",
]
disallow_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_calls = true
warn_return_any = true
strict_equality = true

# ---------------------------------------------------------------- ratchet ของเราเอง
# **ตารางนี้ไม่ใช่ config ของเครื่องมือตัวไหน** — mypy/ruff/coverage ไม่อ่านมัน
# มันคือที่เก็บ "พื้น" ของ ratchet ที่เครื่องมือเจ้าของไม่มีที่ให้เก็บ
# (audit รอบ 14 ข้อ 3: ratchet ที่เขียนเป็นประโยครอดจากตัวทวงของ ADR 0068
# มาได้ เพราะตัวทวงอ่านเฉพาะพื้นที่เป็น *ตัวเลข* ใน config ของเครื่องมือ)
[tool.todolist.ratchets]
# จำนวนโมดูลใน `app/` (ไม่นับส่วนเสริมของ plugin) ที่อยู่ใน strict list ของ mypy
# **ขยับขึ้นได้อย่างเดียว** และต้องขยับใน PR เดียวกับที่เพิ่มโมดูลเข้าลิสต์
mypy_strict_modules = 36

# จำนวนข้อห้ามใน `CLAUDE.md` ที่มีเครื่องบังคับจริง (`tests/test_declared_prohibitions.py`)
# audit รอบ 14 นับได้ 51 ข้อที่เครื่องตรวจได้ · ปิดชุดแรก 8 · รอบ 15 ปิดเพิ่มอีก 3
# (สีดิบใน base.css · User-Agent ของ ZAP · การตัดสาย audit) เหลือ **8 ข้อที่ยังมี
# แค่ประโยค** · รอบ 15 พบว่าไม่มีอะไรทำให้กองนั้นหดลงเลย — ตัวเลขนี้คือตัวทวง
# (ADR 0068: เพดานที่ไม่มีตัวทวง = เพดานที่ไม่ได้ตั้ง) · ลดลงเมื่อไหร่ = ถอดด่านทิ้ง
# · รอบ 18 ขยายทะเบียนให้ครอบข้อบังคับรูป "ทุก X ต้อง Y" ด้วย (11 → 14)
enforced_prohibitions = 14

# coverage ของ `scripts/` — **โค้ดที่บังคับกฎทั้ง 83 gate** (audit รอบ 17 ข้อ 1)
# `[tool.coverage.run] source` ครอบแค่ `app/` มาตลอด ผลคือโค้ด 5,376 บรรทัดที่ทำ
# หน้าที่บังคับทุกอย่าง อยู่นอกทุกเกณฑ์ที่มันบังคับคนอื่น (วัดได้ 43.8% วันที่ตั้ง)
# **วัดแยกไฟล์ข้อมูลใน CI** เพื่อไม่ให้ตัวเลขรวมลากพื้นของแอปลง — ดูขั้นตอนใน ci.yml
# · ข้อ 2 ของรอบเดียวกันพบว่าเลขแรกต่ำกว่าความจริงเพราะ **ไม่นับลูกที่เป็น
# subprocess** ซึ่งคือรูปที่ repo นี้บังคับเองว่าดีกว่า (ดู `.coveragerc-scripts`)
# 65 → 64 (ADR 0077) — **ประชากรที่วัดเปลี่ยน ไม่ใช่การถอย**: `run_gates.py` กับ
# `preflight.py` เคยเป็นโค้ด 368 บรรทัดที่ถูกคุมแน่นด้วยเทสต์ที่ยิงผ่าน subprocess
# ตอนนี้เหลือ adapter ละ 11 บรรทัด ส่วนตัวตรรกะไปอยู่ที่ verifiable-gates ซึ่ง
# ตั้งพื้น coverage ไว้ 100% · ตัวเศษกับตัวส่วนหายไปพร้อมกัน สัดส่วนที่เหลือจึง
# ถูกถ่วงด้วยสคริปต์ที่ยังไม่มีเทสต์มากขึ้น
#
# **จ่ายคืนก่อนลดพื้น**: เติมเทสต์ให้ตัวสั่งงานของ generator ทั้งสอง (`--check`
# ของ build_skill และ `main()` ของ build_agent_skill ซึ่งเป็นตัวเดียวที่เก็บกวาด
# ไฟล์ค้าง) ได้คืนมา 1.04 จุด 63.17 → 64.21 · ที่เหลือคือผลของประชากร ไม่ใช่ของหนี้
#
# **ขั้น 3b — 63 → 62 ด้วยเหตุผลเดียวกับ 3a ทุกประการ**
# `skeleton` · `gh` · `workflows` · `schedule_census` · `red_streak_census`
# ย้ายไป vg · adapter ที่เหลือคุมได้ 84–100% แต่ประชากรเสียโค้ดที่คุมแน่น
# ไป 565 บรรทัด · **นี่เป็นครั้งที่สามติดกันที่ตัวเลขตกเพราะการย้าย ไม่ใช่การถอย**
# ซึ่งยืนยันว่าควรตั้งพื้นใหม่ครั้งเดียวตอนถอดจบ ไม่ใช่ขยับทุกขั้น
#
# **ขั้น 3a (ADR 0077) — 64 → 63 และคราวนี้ *ไม่* จ่ายคืน โดยตั้งใจ**
# `lint_commits` กับ `check_issue_handoff` ย้ายไป verifiable-gates · ประชากรเสีย
# โค้ดที่คุมแน่น 99 บรรทัดแล้วได้ adapter คืนมา 26 (คุม 87.5%/85.7%)
# · **ของที่จ่ายคืนไว้รอบก่อนคือ `lint_commits` เอง ซึ่งเพิ่งย้ายตามไป** —
# จ่ายคืนอีกรอบให้ตัวเลขเดิมยืน คือการเติมเทสต์ให้โค้ดที่กำลังจะออกไป
# · ขั้น 3b–3e จะย้ายอีก ~2,700 บรรทัด **ตัวเลขนี้จึงกำลังตามเครื่องมือวัด
# ไม่ใช่ตามคุณภาพ** · เมื่อถอดจบ ควรตั้งพื้นใหม่จากประชากรที่เหลือจริง
# ครั้งเดียว แทนการขยับทีละขั้น — บันทึกไว้เป็นของค้างของขั้น 5
#
# **ADR 0078 จ่ายคืนอีกรอบด้วยเหตุเดียวกัน** — generator สองตัวข้างบนตามแผ่นกฎไป
# verifiable-gates ทั้งคู่ ประชากรที่วัดจึงเปลี่ยนอีกครั้งและตกไป 63.36 · คราวนี้
# จ่ายคืนที่ `lint_commits.py`: `check_title`/`check_sign_off`/`parse_log` มีเทสต์
# ครบมานาน แต่**ตัวสั่งงานไม่เคยถูกเดินเลย** ทั้งที่ hook `commit-msg` เรียกโหมด
# `--msg-file` ทุกครั้งที่ commit และ job `commit-lint` เรียกโหมด `--range` ทุก push
# · 52.9% → 97.1% ดันภาพรวมกลับเป็น 64.25 · **พื้นจึงไม่ต้องลด**
scripts_coverage = 62

# ---------------------------------------------------------------------------
# **สี่ตัวข้างล่างนี้กันการ *ถอด* ไม่ใช่กันการถอย** (ADR 0069 · audit รอบ 16)
#
# รอบ 16 วัดด้วยการลบของจริง 11 ครั้ง: ทะเบียนที่ถูกตรวจกับ *ความจริง* (คอลัมน์ ·
# route · ไฟล์ · job) ลบไม่ได้เงียบ · ทะเบียนที่ถูกตรวจกับ *กระดาษอีกใบ* ลบได้
# เงียบสนิท เพราะการลบทั้งสองข้างพร้อมกันยังนับว่า "ตรงกัน" อยู่ดี
#
# **โตได้อิสระ (slack ไม่จำกัด) แต่หดต้องมาแก้เลขนี้** — การเพิ่มถูกเฝ้าด้วยด่าน
# อื่นครบอยู่แล้ว (ลงทะเบียนไฟล์เทสต์ · overlay · หลักฐาน) ที่ขาดคือการถอด
# กองที่กันการ *เพิ่ม* — การปิดเครื่องตรวจรายบรรทัด (audit r21)
# ขึ้นได้เฉพาะเมื่อเป็นคำตัดสินที่มีเหตุผลใน commit · ลงแล้วต้องลดเพดานตามทันที
[tool.todolist.ceilings]
# `# noqa` + `# type: ignore` ใน app/ scripts/ tests/
# 95 → 96 ตอน audit รอบ 25: `scripts/sync_counts.py` อ่าน `gates.yaml` จึงต้อง
# มี `# type: ignore[import-untyped]` ของ pyyaml เหมือนทุกตัวที่อ่าน YAML —
# **มีเหตุผลกำกับ** จึงไม่กระทบเพดานของบรรทัดที่ไม่มีเหตุผล (ยังเป็น 48)
# 95 → 88 (ADR 0077): overlays/ กับเทสต์ที่ตามของไป verifiable-gates พาบรรทัด
# ที่ปิดเครื่องตรวจของตัวเองไปด้วย 7 บรรทัด — เพดานต้องลดตามทันที ไม่งั้นที่ว่าง
# ที่เพิ่งได้จะถูกถมกลับโดยไม่มีใครสังเกต
# 88 → 89 (ขั้น 2e): ตัวตรวจคีย์ซ้ำใน workflow ต้องใช้ `yaml.load` กับ loader
# ของตัวเอง ซึ่ง S506 ห้ามไว้ — แต่ loader นั้นสืบทอด `SafeLoader` ทั้งดุ้น
# และแค่เพิ่มการรายงานคีย์ซ้ำ ไม่ได้เปิดชนิดใดเพิ่ม · **มีเหตุผลกำกับ** จึงไม่
# กระทบเพดานของบรรทัดที่ไม่มีเหตุผล (ยังเป็น 43)
# 88 → 84 · ไม่มีเหตุผล 43 → 40 (ขั้น 3a): `lint_commits` กับ
# `check_issue_handoff` ย้ายไป verifiable-gates พร้อม `# noqa` ของตัวเอง
# (S603/S607 ของ `subprocess.run` และ argv ที่คงที่) — ที่ว่างนี้เกิดจากการ
# ถอดของ ไม่ใช่จากการแก้ จึงต้องปิดทันทีเหมือนกัน
#
# 89 → 88 (ADR 0078): รายการยกเว้นของ `overlays/flask/preflight.py` ถูกถอด —
# ไดเรกทอรีนั้นหายไปตั้งแต่ขั้น 2e แต่บรรทัดยกเว้นยังค้าง · ที่ว่างที่เพิ่งได้
# ต้องถูกปิดใน PR เดียวกับที่ทำให้มันดีขึ้น ไม่งั้นมันจะถูกถมกลับเงียบ ๆ
# 84 → 85 (ขั้น 3b): `scripts/skeleton.py` กลายเป็น adapter จึงต้องต่อ path ไป
# `vendor/verifiable-gates/src` ก่อน import ซึ่ง E402 ห้ามไว้ — **บรรทัดนี้มี
# เหตุผลกำกับ** และเป็นรูปเดียวกับ adapter อีกสี่ตัวที่มีอยู่แล้ว · ทางที่ไม่ต้อง
# ปิดกฎคือย้าย `sys.path.insert` ไปโมดูลกลางแล้ว import มันก่อน ซึ่ง**อันตรายกว่า**:
# ruff จัดกลุ่ม `verifiable_gates` เป็น third-party และ local เป็นกลุ่มถัดไป
# การเรียงอัตโนมัติจึงย้าย import ขึ้นเหนือตัวต่อ path แล้วพังเงียบ ๆ
suppressions = 85
# ในจำนวนนั้น กี่บรรทัดที่ไม่มีข้อความบอกเหตุผลต่อท้าย
# 48 → 43 ด้วยเหตุผลเดียวกัน
# 40 → 37 (ขั้น 3b): ตัวอ่านกับสำมะโนที่ย้ายไป verifiable-gates พาบรรทัดที่ปิด
# เครื่องตรวจแบบไม่มีเหตุผลไปด้วยสามบรรทัด — ที่ว่างที่เกิดจากการถอดของ ต้องถูก
# ปิดทันทีเหมือนกัน
suppressions_without_reason = 37
# แถวใน docs/EXTERNAL-SURFACE.md ที่ยอมรับตรง ๆ ว่ายังไม่มีใครเทียบกับของจริง
# (ADR 0072) — วัดตอนตั้งเพดาน 2026-08-22 · ผิวนอกโตทุกครั้งที่ GitHub เพิ่ม
# setting ใหม่ และของใหม่เกิดมาไม่มีเจ้าของเสมอ
# 18 → 19 (2026-08-25): repo placeholder `verifiable-gates` เกิดตาม ADR 0075 ข้อ 6 —
# ผิวนอกชิ้นใหม่ที่ยังไม่มีเครื่องตรวจ จนกว่ามันจะมี posture ของตัวเอง
external_surface_unowned = 19
# จำนวน gate ทั้งหมดใน gates.yaml — เพดาน*บน* (ADR 0075 ข้อ 3) · กติกาเดิมกันแค่การถอด
# (`removals.gates_total`) แต่ไม่มีอะไรกันการโต: 109 → 116 ใน 6 วัน ขณะที่หลักฐาน
# `proved_by` ครอบ 44/116 และการทดลองเฟส 12 บอกว่ากฎเพิ่มให้ผลตอบแทนลดลง ·
# gate ใหม่ต้องมาพร้อมการถอด gate เก่า หรือ ADR ที่ขยับเลขนี้
# 116 → 115 (ADR 0077): `overlay-covers-every-portable-gate` ตามของไป
# verifiable-gates แล้ว — คำอ้างของมันเป็นเรื่องของ overlay ซึ่ง repo นี้ไม่มีอีก
# 115 → 114 (ADR 0078): `skill-mirrors-portable-gates` ไม่มีอะไรให้บังคับที่นี่แล้ว
# เพราะแผ่นกฎย้ายไป vg — และคลังที่เผยแพร่กฎซึ่งไม่มีใครบังคับ ถูกด่านทิศที่สอง
# ของ `test_rule_catalogue_agreement.py` จับได้ตั้งแต่รันแรก
gates_ceiling = 114

[tool.todolist.removals]
# gate ทั้งหมดใน gates.yaml — ถอด gate ทิ้งเคยเขียวได้ทั้ง CI ถ้าเก็บกวาดครบ 6 ที่
gates_total = 109
# แถวใน "ส่วนที่ต้องมีคนลงมือ" ของ docs/SECURITY-CADENCE.md
cadence_rows = 32
# แถวในทะเบียนความเสี่ยงของ docs/RISK-ASSESSMENT.md
risk_rows = 13
# แถวในทะเบียน "การตัดสินใจที่จงใจเลื่อน" ของ docs/GOVERNANCE.md
deferred_rows = 2

# ---------------------------------------------------------------- coverage
[tool.coverage.run]
source = ["app"]
branch = true
omit = ["app/sun_data.py"]  # ตาราง generate — ไม่มี logic ให้ cover

[tool.coverage.report]
show_missing = true
# ตั้งจากค่าที่วัดจริงตอน Phase 0 แล้วห้ามต่ำลง (ratchet ขึ้นได้อย่างเดียว)
fail_under = 97  # ขยับขึ้นได้อย่างเดียว · **และมีตัวทวงให้ขยับแล้ว** (audit รอบ 12):
# `scripts/check_ratchets.py` แดงเมื่อพื้นห่างจากของจริงเกิน 1 จุด — ก่อนหน้านี้พื้น
# ค้างที่ 96 ตั้งแต่ 2026-08-12 (ตอนนั้นวัดได้ 96.31%) ขณะที่ของจริงไต่ไป 97.11%
# **ตอนปิดเฟสวัดได้ 96.18% ซึ่งลดจาก 96.58% ของ Phase 6** — ไล่ดูแล้วพบว่า
# กิ่งที่ขาดคือ *พฤติกรรมที่เฟสนี้อ้างเองว่าสำคัญ* (แถวล็อกของสาย audit หายไป
# ต้องดัง · plugin ตัวเดียวพังต้องไม่ล้มทั้งคำขอ) — อ้างแล้วไม่เทสต์คือช่องว่าง
# แบบเดียวกับที่เฟสนี้ไล่ปิดมาตลอด จึงเขียนเทสต์ให้แล้วขึ้นมาที่ 96.31%
# **ยังไม่ขยับเพดาน** เพราะที่ว่างเหลือ 0.31 จุด ซึ่งต่ำกว่านโยบาย ~1 จุด
# **ไม่ขยับเป็น 96.5** เพราะเลขที่ชิดค่าที่วัดได้ทำให้การเพิ่มโค้ดที่ถูกต้อง
# แต่ยังไม่มีเทสต์ (เช่นเส้นทาง error ของ backend ตัวใหม่) กลายเป็นงานที่ต้อง
# หยุดทำ ratchet มีไว้กันการถอยหลัง ไม่ใช่กันการเดินหน้า
# (คงที่ 96 ไม่ใช่ลืมขยับ — เฟสนี้เพิ่มโค้ดของ registry/CLI ที่มีกิ่งของ
#  "ไม่มีไลบรารี/ไม่ได้ถูกเลือก" ซึ่งเดินได้เฉพาะบางสภาพแวดล้อม เพดานจึงยังไม่ขยับ)

# ---------------------------------------------------------------- interrogate
[tool.interrogate]
# **ไม่มีคีย์ `paths` ที่นี่** — interrogate รับ path จาก command line เท่านั้น
# ใส่ไว้ก็ถูกเมิน (เคยใส่แล้วเข้าใจผิดว่าจำกัดขอบเขตให้แล้ว) ขอบเขตจริงคือ
# `interrogate app` ใน .github/workflows/ci.yml — รันเปล่า ๆ จะกวาดทั้ง repo
exclude = ["app/sun_data.py"]
ignore-init-method = true
ignore-nested-functions = true
# ตั้งจากค่าที่วัดจริงตอน Phase 0 (ratchet เช่นกัน)
fail-under = 85  # ขยับขึ้นได้อย่างเดียว · มีตัวทวงเหมือน coverage (audit รอบ 12 · ของจริง 85.5%)
# **Phase 6 กินที่ว่างไปแล้วคืนเอง**: `app/metrics.py` ที่เพิ่มเข้ามาทำให้ตกไป
# ที่ 84.3% (เหลือที่ว่างแค่ 0.3 จุด) จึงเขียน docstring ของโมดูลนั้นให้ครบ
# แล้วกลับมาที่ 84.7% — **เฟสที่กินที่ว่างคือเฟสที่ต้องคืน** ไม่ใช่ปล่อยให้
# เฟสถัดไปเจอ CI แดงตอนที่กำลังแก้เรื่องอื่นอยู่
# **ตั้งต่ำกว่าค่าที่วัดได้ ~1 จุด โดยตั้งใจ** เพดานที่ชนค่าที่วัดได้พอดี (80 กับ
# 80.3) ทำให้ฟังก์ชันใหม่ที่ไม่มี docstring แค่สองตัวก็ทำ CI แดง ซึ่งจะแดงตอนที่
# คนกำลังแก้เรื่องอื่นอยู่ — ที่ว่างตรงนี้คือที่ให้งานปกติเดินได้ ไม่ใช่ที่ให้
# ปล่อยผ่าน ถ้าถูกกินจนหมดแปลว่าถึงเวลาเขียน docstring ชุดถัดไปแล้วขยับเพดานตาม
