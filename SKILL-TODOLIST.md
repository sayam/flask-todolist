# SKILL-TODOLIST — ข้อตกลงของตัวแอป (ชั้น business)

**ไฟล์นี้ generate มา ห้ามแก้ด้วยมือ** — สร้างใหม่ด้วย
`python scripts/build_skill.py` · แหล่งจริงคือ gate ที่ `layer: business`
และ `portable: true` ใน `gates.yaml`

**ใบนี้ต่อยอด `SKILL.md` (baseline) — ต้องรับ baseline ก่อนจึงรับใบนี้ได้**
เพราะข้อตกลงข้างล่างถูกเขียนบนสมมติฐานว่าวินัยพื้นฐาน (mutation test ·
ด่านสองทิศ · ratchet · ADR) มีอยู่แล้ว (ADR 0042)

ต่างจาก baseline ตรงไหน: กฎในใบนี้เป็น**ทางเลือกของแอปชนิด todolist** —
แอปอื่นเลือกต่างได้โดยไม่ถือว่าบกพร่อง (เช่น แอปที่กฎหมายบังคับ erasure จริง
ย่อมไม่ใช้ soft delete) · การทดลองใน `docs/comparison/` ชี้ว่ากฎชั้นนี้แหละ
คือสิ่งที่ scaffolding ให้แล้วการทบทวนเองให้ไม่ได้ — เพราะมันเดาไม่ได้
ถ้าไม่มีใครเขียนไว้

## กฎ

แต่ละข้อ: **กฎ** (ของ app-type นี้ ไม่ผูก framework) · **เกิดจาก** (กับดักจริง)
· **ตัวบังคับใน reference** (ของ repo นี้)

### `delete-means-soft-delete`

**กฎ:** ลบ = ซ่อน (soft delete) — ลบจริงได้ที่ purge เท่านั้น

**เกิดจาก:** ADR 0014 — `--dry-run` ที่ implement เป็น rollback เคยลบข้อมูลจริง เพราะตัว purge commit ก่อน savepoint ปิด · ที่ลบได้จึงต้องมีที่เดียว

**ตัวบังคับใน reference:** `tests/test_write_discipline.py` · `tests/test_soft_delete.py`

### `every-write-audited`

**กฎ:** ทุกการเขียนลง audit trail แบบเติมได้อย่างเดียว + hash chain

**เกิดจาก:** ADR 0015/0035 — event หลัง flush ดักให้เองทุก insert/update/delete โค้ดฟีเจอร์ไม่ต้องเรียกอะไร เพราะ "ลืมเรียก" คือบั๊กที่เงียบที่สุด

**ตัวบังคับใน reference:** `tests/test_audit.py`

### `ropa-current`

**กฎ:** ROPA + บัญชี log + runbook ตรงกับระบบจริง

**เกิดจาก:** แอปที่ประมวลผลข้อมูลส่วนบุคคลต้องตอบได้ว่าเก็บอะไร ที่ไหน นานเท่าไหร่ — ROPA ที่ไม่ตรงระบบจริงคือเอกสารที่ผิดเงียบ ๆ · ยกขึ้น business ตาม ADR 0057

**ตัวบังคับใน reference:** `tests/test_ropa.py`

### `design-doc-matches-the-ui`

**กฎ:** docs/DESIGN.md ตรงกับ UI จริง — ทุกหน้าเต็มถูกตัดสินโหมด และตัวแปรสีตรงกับ base.css สองทิศ

**เกิดจาก:** CR#4 — caption ลอยกึ่งกลางหลายหน้าเพราะแนวอ่านของทั้งหน้าไม่เคยถูกเขียน หน้าใหม่จึงเกิดตามค่าเริ่มต้นของ browser · เอกสาร design ที่ไม่มีด่าน คือเอกสารที่ล้าสมัยแล้วพาหน้าถัดไปหลงทาง

**ตัวบังคับใน reference:** `tests/test_design_doc.py`

### `admin-masking-by-classification`

**กฎ:** หน้า admin เห็นข้อมูลผู้ใช้แบบ mask ตามชั้นข้อมูล และ unmask ต้องลง audit

**เกิดจาก:** ADR 0045 (เฟส 14-02) — ชั้นข้อมูลที่ไม่มีผลกับสิ่งที่ admin เห็นบนจอ คือชั้นข้อมูลบนกระดาษ · ยกเป็น portable ได้ต่อเมื่อมี checker ของ overlay ที่ตรวจเรื่องนี้ในโปรเจกต์อื่นได้จริง

**ตัวบังคับใน reference:** `tests/test_masking.py`

### `admin-panels-read-real-state`

**กฎ:** panel ระบบของ admin อ่านจาก runtime/ดิสก์จริง ตอบตามจริง และ admin เท่านั้น

**เกิดจาก:** เฟส 14-04/14-06 — บทเรียนรอบตรวจเอกสาร: เลขที่ไม่ได้อ่านจากของจริงคือ เลขที่ผิดอยู่แล้ว หน้า admin ที่แสดงสถานะระบบจึงต้องถูกเทียบกับแหล่งจริง และของที่อ่านไม่ได้ต้องพูดตรง ๆ ไม่ใช่เดา

**ตัวบังคับใน reference:** `tests/test_admin_panels.py`

### `secrets-encrypted-at-rest`

**กฎ:** ความลับที่ต้องอ่านคืนได้ ถูก encrypt บนดิสก์ และคีย์หาย/ผิดต้องดังพร้อมเหตุผล

**เกิดจาก:** ADR 0046 (เฟส 15) — dump ฐานข้อมูลใบเดียวเคยได้ความลับ TOTP ทั้งหมด เพราะมันเป็น C1 ตัวเดียวที่ต้องเก็บค่าจริง · การถอดต้องล้มดังเมื่อคีย์ หาย/ผิด ไม่ใช่คืนขยะ — และข้อความสองกรณีนั้นต้องบอกทางแก้คนละทาง

**ตัวบังคับใน reference:** `tests/test_totp_encryption.py`

### `legal-pdpa-worksheet-honest`

**กฎ:** worksheet PDPA อ้างหลักฐานที่มีจริงทุกชิ้น และช่องว่างทุกข้ออยู่ใน backlog

**เกิดจาก:** pilot ของชั้น legal (ADR 0042 · เฟส 13-04) — เอกสาร map กฎหมายที่ไม่มี ใครตรวจหลักฐาน จะถูกใช้ตัดสินใจในวันที่มีเรื่องจริง ซึ่งสายเกินแก้

**ตัวบังคับใน reference:** `tests/test_pdpa.py`

### `suite-on-three-brands`

**กฎ:** ชุดเทสต์ทั้งชุดผ่านบน MySQL 8 และ MariaDB 11 จริง ไม่ใช่แค่ SQLite

**เกิดจาก:** "รองรับหลายยี่ห้อ" ที่ไม่เคยยิงยี่ห้อจริงคือคำโฆษณา — deadlock ของสาย audit เจอได้เฉพาะบน InnoDB จริงเท่านั้น (ADR 0032/0035)

**ตัวบังคับใน reference:** job `dialects`
