# รอบการตรวจสอบความปลอดภัย

Phase 7 · P7-04 — **นโยบายนี้มีเทสต์คุม** (`tests/test_cadence.py`)
เอกสารที่บรรยายรอบซึ่งไม่มีใครเดินตามคือเอกสารที่ทำให้เชื่อว่ามีคนตรวจอยู่
ทั้งที่ไม่มี · ตารางข้างล่างจึงถูกอ่านด้วยเทสต์จริง และเลยกำหนดเมื่อไหร่ CI แดง

## ส่วนที่รันเองทุก push — ไม่ต้องมีใครจำ

| job | ตอบคำถามอะไร | แดงได้ไหม |
|---|---|---|
| `test` · `dialects` · `bare` | โค้ดยังทำงานถูกบนทุกยี่ห้อฐานข้อมูล และในสภาพที่ไม่มีไลบรารีของ plugin | แดง |
| `lint` | ruff · ruff format · xenon · interrogate · mypy · ตาราง ASVS ตรงกับมาตรฐานที่ตรึงไว้ | แดง |
| `security` | pip-audit ของ **core และ `[deploy]`** — ช่องโหว่ของสิ่งที่ถอดไม่ได้ · semgrep | แดง |
| `codeql` | SAST ของโค้ดที่เราเขียนเอง (python + javascript, ชุด `security-extended`) — ต่างจาก `security` ที่ดูไลบรารีของคนอื่น | แดง |
| (กติกาคู่ `codeql`) | **ห้าม PEP 695 ใต้ `app/`** จนกว่า extractor จะอ่านได้ (`tests/test_codeql_compat.py`) — ตอน bump รุ่น CodeQL ให้ลองถอดด่านนี้แล้วดูหน้า status ว่า warning "Could not process" หายจริงก่อนถอดถาวร | แดง |
| `scorecard` | OpenSSF Scorecard — วัดแนวปฏิบัติของ *repo* (branch protection, pinned deps, สิทธิ์ของ token) ไม่ใช่โค้ด · อยู่คนละไฟล์ workflow เพราะต้องการ trigger `branch_protection_rule` + `schedule` | **ไม่บล็อก** — เป็นคะแนน ไม่ใช่ผ่าน/ไม่ผ่าน และไม่รันบน PR |
| `plugin-audit` | pip-audit ของ **plugin** | **ไม่แดง** ตั้งใจ — คำตอบของ CVE ที่ถอดได้คือ "ถอดก่อน" จึงเตือนแทนที่จะบล็อก |
| `sbom` | รายการไลบรารีทั้งหมดที่ประกอบเป็น image | แดง |
| `secret-scan` | ความลับหลุดเข้า git หรือยัง — **เฉพาะ commit ที่อยู่ใน push ครั้งนั้น** ส่วน "ตลอดประวัติมีอะไรหลุดไปแล้วหรือยัง" เป็นคนละคำถาม อยู่ในตารางถัดไป | แดง |
| **push protection** (ของ GitHub ไม่ใช่ job) | ปฏิเสธ push ที่มีความลับ **ก่อนมันเข้า repo** — ครอบชั้นที่ `secret-scan` ครอบไม่ถึง เพราะตัวนั้นตรวจหลังของเข้าไปแล้ว ซึ่งสายเกินไปโดยนิยาม | บล็อกการ push |
| `dast` | ZAP baseline ยิง stack ที่รันจริง **แบบที่ login แล้ว** | แดง |
| `a11y` | pa11y-ci บน Chromium จริง (โหมดมืด · ธีม ocean · ภาษาไทย) | แดง |
| `stack` · `sso` · `ldap` · `vault` · `image` · `purge-timer` · `schema` · `openapi` | การต่อสายของจริงทั้งหมด | แดง |
| `siem` | log ถึง Loki จริง · กฎแจ้งเตือนย่อยได้และ**ดังจริง**เมื่อยิงเหตุการณ์ใส่ (ADR 0037) | แดง |
| `n-1` | โค้ดของ tag ล่าสุดให้บริการบน schema ของ HEAD ได้จริง — สัญญา rolling (ADR 0048) | แดง |
| `scrape` | Prometheus ดูด `/metrics` ผ่านด่าน token จริงและตัวเลขถึง TSDB · ไม่มี token ต้อง 401 | แดง |
| `scaffold` | overlay ติดตั้งลง repo เปล่าได้จริง และ repo นี้ผ่าน scan ของ overlay ตัวเอง (dogfood) | แดง |

**สิ่งที่ส่วนนี้ตอบไม่ได้**: ทุก job ข้างบนตรวจสิ่งที่มีคนคิดไว้แล้วว่าต้องตรวจ
มันจับ*การถอยหลัง*ได้ดีมากและจับ*ของที่ไม่มีใครเคยคิดถึง*ไม่ได้เลย —
นั่นคือหน้าที่ของส่วนถัดไป

## ส่วนที่ต้องมีคนลงมือ — และตารางนี้ถูกเทสต์อ่าน

รูปแบบของช่อง "ครบกำหนด": เป็น **วันที่** (`YYYY-MM-DD`) หรือ **เงื่อนไข**
ที่ขึ้นต้นด้วย `เมื่อ` · เทสต์ตรวจเฉพาะแถวที่เป็นวันที่ ส่วนแถวที่เป็นเงื่อนไข
ต้องเขียนเงื่อนไขให้คนอื่นตัดสินได้ว่ามันเกิดขึ้นแล้วหรือยัง

| การตรวจ | รอบ | ครั้งล่าสุด | ครบกำหนด | หลักฐานอยู่ที่ไหน |
|---|---|---|---|---|
| ทบทวนตาราง ASVS ทั้งใบเทียบกับของจริง (พ่วง PDPA และ ISO 27001 ตามที่สองไฟล์นั้นประกาศ) | 12 เดือน | 2026-08-11 | 2027-08-11 | [ASVS.md](ASVS.md) · [PDPA.md](PDPA.md) · [ISO27001.md](ISO27001.md) |
| ทบทวนเงื่อนไขหมดอายุของ ADR 0033 (บังคับ MFA หรือยัง) | 12 เดือน | 2026-08-11 | 2027-08-11 | [ADR 0033](adr/0033-mfa-is-offered-not-required.md) ข้อ 3 |
| ทบทวนช่องโหว่ที่ค้างอยู่และของที่ pin ไว้ | 3 เดือน | 2026-08-11 | 2026-11-11 | ผลของ job `security` และ `plugin-audit` |
| ขยับเวอร์ชันมาตรฐาน ASVS ถ้ามีรุ่นใหม่ | 12 เดือน | 2026-08-11 | 2027-08-11 | `scripts/build_asvs_worksheet.py --fetch` |
| WCAG 2.2 AA ตรวจด้วยมือเต็มรอบ | 12 เดือน | 2026-08-12 | 2027-08-12 | [ACCESSIBILITY-AUDIT.md](ACCESSIBILITY-AUDIT.md) |
| ประเมินความเสี่ยงเต็มรอบตามวิธีที่ประกาศ | 12 เดือน | 2026-08-16 | 2027-08-16 | [RISK-ASSESSMENT.md](RISK-ASSESSMENT.md) |
| ซ้อม restore บนสภาพ deploy จริง (อัตโนมัติซ้อมทุก push อยู่แล้ว) | 12 เดือน | 2026-08-16 | 2027-08-16 | [RUNBOOK-BACKUP.md](RUNBOOK-BACKUP.md) |
| pentest ด้วยมือ (คนนอกทีม) | ทุก major release และอย่างน้อยปีละครั้ง | ยังไม่เคยทำ | เมื่อจะเปิดให้คนนอกเข้าถึงระบบเป็นครั้งแรก | ยังไม่มี |
| กวาดหาความลับใน**ประวัติทั้งหมด** | 12 เดือน และหลังเขียนประวัติใหม่ทุกครั้ง | 2026-08-12 | 2027-08-12 | `scripts/secret_scan_history.sh` · ข้อยกเว้นใน `.gitleaksignore` |
| ทบทวน alert ของ CodeQL ที่ถูก dismiss ไว้ | 6 เดือน | 2026-08-12 | 2027-02-12 | `gh api repos/sayam/flask-todolist/code-scanning/alerts?state=dismissed` |
| ซ้อมใช้ runbook ตอนข้อมูลรั่ว | 12 เดือน | ยังไม่เคยทำ | เมื่อมีข้อมูลจริงในระบบพอที่การซ้อมจะบอกอะไรได้ — ดู [RUNBOOK-BREACH.md](RUNBOOK-BREACH.md) ข้อ 7 | [RUNBOOK-BREACH.md](RUNBOOK-BREACH.md) |

**เลยกำหนดแล้วทำอย่างไร**: ทำการตรวจนั้นแล้วขยับวันที่ **หรือ** เลื่อนออกไป
อย่างเปิดเผยโดยแก้ตารางนี้พร้อมเหตุผล — ทั้งสองทางเป็น commit ที่มีคนเห็น
ห้ามแก้เทสต์ให้มันเงียบ (หลักเดียวกับ `ALLOWED_LINES` ใน
`tests/test_write_discipline.py`)

## Branch protection ของ `main` (2026-08-13)

บังคับ PR · ต้องผ่าน **ครบทั้ง 27 check** · linear history · ห้าม force push
และห้ามลบ branch

**`enforce_admins: false` ยกเว้นเจ้าของจาก *ทุกข้อ* ข้างบน ไม่ใช่แค่ข้อบังคับ PR**
— เจ้าของยัง force push และลบ branch ได้ด้วย · เขียนให้ตรงตรงนี้เพราะการเข้าใจ
ว่า "force push ถูกกันแล้ว" ทั้งที่กันเฉพาะคนอื่น คือความมั่นใจที่ผิดที่ผิดทาง
· กฎทั้งชุดมีผลเต็มที่กับคนนอก ซึ่งเป็นเหตุผลที่เปิดมันตั้งแต่แรก

- **ต้อง approve 0 คน** เพราะโปรเจกต์นี้มีคนดูแลคนเดียวและ GitHub ไม่ให้ approve
  PR ของตัวเอง — ตั้งเป็น 1 เมื่อไหร่ = merge ไม่ได้ตลอดกาล · ตั้งเป็น 1 ตอนที่
  มีคนที่สองจริง ๆ ไม่ใช่ตอนที่อยากให้ดูเข้ม
- **ชื่อ check ต้องตรงกับที่ CI สร้างเป๊ะ** — พิมพ์ผิดตัวเดียวได้ด่านที่ไม่มีวัน
  ผ่าน (PR ค้างตลอดกาล) ส่วน check จริงที่ตกหล่นจากลิสต์คือด่านที่ไม่กันอะไรเลย
  · **เพิ่ม job ใหม่ต้องมาเติมที่นี่ด้วย** ไม่งั้นมันจะรันแต่ไม่กัน
- **บังคับ 26 จาก 27 check** — ตัวที่ไม่บังคับคือ `scorecard` ซึ่ง**ไม่รันบน
  pull request** เลย (trigger เป็น push/schedule/branch_protection_rule)
  การบังคับจึงเท่ากับให้ PR ทุกใบรอ check ที่ไม่มีวันมา · และมันเป็น *คะแนน*
  ไม่ใช่ผ่าน/ไม่ผ่าน — ด่านที่ไม่มีเส้นแบ่งชัดไม่ควรเป็นด่านที่บล็อกคน
- ปิด merge commit ที่ระดับ repo ด้วย เพราะ linear history จะปฏิเสธมันเสมออยู่แล้ว
  — ปุ่มที่กดแล้วพังทุกครั้งคือกับดัก ไม่ใช่ทางเลือก
- **การห้าม force push จะขวางการเขียนประวัติใหม่** แบบที่ทำไปเมื่อ 2026-08-12
  สำหรับทุกคนที่ไม่ใช่ admin · วันที่ตั้ง `enforce_admins: true` (ควรตั้งเมื่อมี
  คนที่สอง) ข้อนี้จะขวางเจ้าของด้วย ต้องปลดชั่วคราวแล้วตั้งกลับ — เขียนไว้ตรงนี้
  เพราะวันนั้นจะหาไม่เจอว่าอะไรขวางอยู่

## Dependabot — และสิ่งที่มันมองไม่เห็น

เปิด **security updates** ไว้ (2026-08-12) — มันเปิด PR ให้เองเมื่อมี CVE
ของ dependency

**version updates เปิดเฉพาะของที่ถูกตรึงไว้** — GitHub Actions, base image ของ
Docker และเครื่องมือใน `pins/` · เส้นแบ่งคือ **ของที่ตรึงไว้ที่รุ่นหนึ่งอย่าง
เจาะจงต้องมีคนขยับให้** ไม่งั้นคือการแช่ช่องโหว่ไว้ ส่วน `Pipfile.lock` ของแอป
ไม่ได้ถูกตรึง (ประกาศเป็น `"*"`) และมีร้อยกว่า package — เปิดแล้วจะมี PR แทบทุกวัน
· `tests/test_dependabot.py` บังคับเส้นแบ่งนี้ด้วย **path ไม่ใช่ชื่อ ecosystem**

### action ทุกตัวถูก pin ด้วย commit SHA (2026-08-13)

**tag ย้ายได้ commit ย้ายไม่ได้** — `@v7` ชี้ไปที่ไหนก็ได้ที่เจ้าของ action
ย้ายมันไป และ action รันด้วยสิทธิ์ของ workflow เรา วันที่บัญชีของเขาถูกยึด
tag เดิมจะชี้ไปโค้ดใหม่โดยที่ไฟล์ในนี้ไม่เปลี่ยนสักตัวอักษร

รูปแบบคือ `uses: owner/repo@<sha40> # vX.Y.Z` — คอมเมนต์ไม่ใช่ของประดับ
มันคือสิ่งเดียวที่บอกคนอ่านว่าใช้รุ่นไหน และเป็นสิ่งที่ Dependabot อ่าน
· `tests/test_workflow_pinning.py` บังคับทั้งสามข้อ: pin ครบ · มีคอมเมนต์เลขรุ่น
· และ **action เดียวกันต้องเป็น SHA เดียวกันทุกที่** (ข้อหลังมาจากของจริง —
`upload-artifact` เคยค้างที่ `@v4` อยู่จุดเดียวใน job `dast` สิบวันโดย CI เขียวตลอด)

**tag แบบ annotated ต้อง deref อีกชั้น** — `git/ref/tags/<tag>` ของ
`github/codeql-action` กับ `ossf/scorecard-action` คืน object ชนิด `tag`
ไม่ใช่ `commit` · เอา SHA นั้นไปใส่ตรง ๆ workflow จะพัง

**base image ของ `Dockerfile` ก็ pin ด้วย digest ด้วยหลักเดียวกัน** — และ
**ราคาที่มันเรียกเก็บต่างจาก action**: pin แล้ว security patch ของ base จะไม่มา
เองอีก ซึ่ง**แย่กว่าเดิม**ถ้าไม่มีใครขยับ · จ่ายด้วยการเปิด
`package-ecosystem: docker` ให้ Dependabot เปิด PR ขยับ digest แทน patch จึงยัง
มาเหมือนเดิม แค่มาเป็น PR ที่มีคนเห็นและผ่าน 27 check · `tests/test_dockerfile_pinning.py`
บังคับว่า **สองอย่างนี้ต้องมาคู่กัน** — ถอด entry ของ Dependabot ออกเมื่อไหร่ต้องแดง
เพราะ pin โดยไม่มีใครขยับคือการแช่ช่องโหว่ไว้

**ต้อง pin digest ของ manifest index ไม่ใช่ของ image ต่อสถาปัตยกรรม** —
pin ผิดตัวจะล็อก build ไว้ที่ arch เดียวโดยไม่มี error จนกว่าจะ build บนเครื่อง
คนละ arch (digest ที่ใช้อยู่ครอบ 7 แพลตฟอร์ม รวม `linux/amd64` ที่ CI ใช้)

### semgrep พิสูจน์ได้ว่าสแกนอะไรไปบ้าง ไม่ใช่แค่ว่าไม่เจออะไร (2026-08-13)

`semgrep scan --quiet --error` ที่ผ่าน แปลว่า "ไม่เจอ" ซึ่ง**หน้าตาเหมือนกันเป๊ะ
กับ "ไม่ได้ตรวจ"** — ตัวตัดสินย้ายไปที่ `scripts/check_semgrep.py` ซึ่งอ่าน
*รายงาน* (`--json --time`) แล้วเทียบ **เซตของไฟล์ที่สแกนจริง กับเซตที่ควรถูก
สแกน** (`git ls-files '*.py'` ลบด้วย `.semgrepignore`) ไม่ใช่ตัวเลขขั้นต่ำลอย ๆ
· เลขขั้นต่ำจับ "หายไปทั้งก้อน" ได้ แต่จับ "หายไปหนึ่งไดเรกทอรี" ไม่ได้

**และนั่นคืออาการของบั๊กจริงที่การวัดครั้งนี้เจอ**: ค่าเริ่มต้นในตัว semgrep
ตัด `tests/` ทิ้ง — **61 จาก 136 ไฟล์ไม่เคยถูกสแกนเลย** ขณะที่คำสั่งใน `ci.yml`
เขียนว่าตัดแค่ `migrations` กับ `.venv` · ไม่มีใครโกหก แต่ไม่มีใครวัด
· วัดแล้วว่ารวม `tests/` เข้ามาได้ **0 finding** การไม่สแกนจึงไม่ได้ประหยัดอะไร
นอกจากทำให้ตัวเลขดูดีกว่าที่เป็น

ขอบเขตย้ายไป **`.semgrepignore` ที่ commit ไว้** (หลักเดียวกับ ADR 0037: กฎอยู่
ในไฟล์ ไม่ใช่ค่าเริ่มต้นที่มองไม่เห็น) · ไฟล์นั้น **แทนค่าเริ่มต้นทั้งชุด ไม่ใช่
เพิ่มจากมัน** — `.venv/` กับ `node_modules/` จึงต้องเขียนเองที่นั่น

`tests/test_semgrep_gate.py` คุมด่านนี้อีกชั้น: รายงานที่ semgrep เขียนกับที่
ตัวตรวจอ่านต้องเป็นไฟล์เดียวกัน · ต้องมี `--json --time` · **ต้องไม่มี `--error`**
(มันทำให้ semgrep ตายก่อนถึงตัวตรวจ ตัวตัดสินต้องมีตัวเดียว) · และ `tests/`
ต้องไม่ถูกตัดออกอีก

### เครื่องมือที่ job ติดตั้งเอง ถูกตรึงด้วย hash (2026-08-13)

ชั้นที่สาม ต่อจาก action (SHA) และ base image (digest) — คราวนี้เป็นของที่
**คำสั่งใน job ไปดึงมาเอง**: `pip install pipenv` และ `npm install pa11y-ci`
ทั้งคู่หยิบของ ณ วินาทีที่รัน และทั้งคู่รันด้วยสิทธิ์ของ workflow เรา
ล็อกไฟล์อยู่ใน `pins/` (ดู `pins/README.md`) · `tests/test_ci_pinning.py` บังคับ

**สองอย่างที่เลขรุ่นอย่างเดียวให้ไม่ได้**:
- `--require-hashes` ผูกกับ *ไบต์* ไม่ใช่ชื่อรุ่น
- และมันปฏิเสธด้วยถ้ามี dependency ตัวไหน **ไม่ได้ถูกระบุไว้** — `npm install
  pa11y-ci@4.1.1` ตรึงได้แค่ตัวมันเอง อีก 161 package ที่ตามมายังเป็น
  "ล่าสุดที่เข้าช่วง" ทุกครั้ง · `npm ci` อ่านจาก `package-lock.json` ซึ่งมี
  integrity hash ครบทั้งต้นไม้ และ **ล้มถ้า lock ไม่ตรงกับ package.json**
  ต่างจาก `npm install` ที่จะแก้ lock ให้เงียบ ๆ แล้วเดินต่อ

ราคาที่จ่ายเป็นตัวเดียวกับ base image และจ่ายด้วยวิธีเดียวกัน: Dependabot
เฝ้าทุกไดเรกทอรีใน `pins/` และเทสต์บังคับว่า **เพิ่ม pin แล้วลืมต่อ Dependabot
= แดง** · ไฟล์ล็อกเป็นผลของ `pip-compile --generate-hashes` ซึ่ง Dependabot
compile ใหม่ให้เอง — `requirements.in` จึงต้องถูก commit ไปด้วย

**pin ของ CI ไม่ได้ครอบ service container** (`redis:7`, `mysql:8`, `mariadb:11`)
โดยตั้งใจ — นั่นคือของที่เรา *ทดสอบว่าเข้ากันได้* ไม่ใช่ของที่เรา *ส่งมอบ*
ตรึงไว้แปลว่าจะไม่มีวันรู้ว่ารุ่นถัดไปทำให้เราพัง ซึ่ง job `dialects` มีไว้เพื่อจับ

### merge PR ของ Dependabot ด้วย **rebase** ไม่ใช่ squash

squash ต่อท้ายหัว commit ด้วย ` (#N)` และหัวของ Dependabot ยาวอยู่แล้ว —
PR ใบแรก (2026-08-13) หัวยาว 84 ตัว squash แล้วได้ 89 ซึ่ง**เกินเพดาน 72 ของ
`scripts/lint_commits.py`** แล้ว `commit-lint` จะแดงบน `main` หลัง merge ไปแล้ว
(สายเกินกว่าจะกันได้) · ส่วน commit จริงในกิ่งสั้นกว่าเสมอ rebase จึงใช้ตัวนั้น

ยังไม่ได้ปิดปุ่ม squash ที่ระดับ repo เพราะมันมีประโยชน์กับ PR ของคนที่ commit
กระจัดกระจาย — **ถ้าวันหนึ่งพลาดเรื่องนี้ซ้ำ ให้ปิดมันทิ้ง** ปุ่มที่ต้องจำว่า
"ห้ามกดกับ PR แบบนี้" คือปุ่มที่จะถูกกดสักวัน

**ข้อจำกัดที่วัดแล้ว ไม่ใช่ที่เดา:** dependency graph ของ GitHub อ่าน Pipfile
เฉพาะ `[packages]` กับ `[dev-packages]` — ตรวจเมื่อ 2026-08-12 เห็น 126 package
และ **ไม่เห็น `ldap3`, `hvac`, `redis`, `pymysql`, `segno` และ `gunicorn` เลย
สักตัว** แปลว่า Dependabot จะไม่มีวันเตือนเรื่องของพวกนี้ให้

นี่คือ**ราคาที่ ADR 0025 จ่าย** เพื่อให้ "ถอด plugin แล้ว supply chain หายไปด้วย"
เป็นจริง: เครื่องมือภายนอกที่รู้จักแค่สอง category มาตรฐานจะมองไม่เห็นของที่เหลือ
· ช่องนั้นถูกปิดด้วยกลไกของเราเอง — job `plugin-audit` (ต่อ category, เตือนไม่แดง)
และ job `security` ที่ตอนนี้ audit `[deploy]` ด้วย

**`[deploy]` เคยเป็นช่องว่างจริงจนถึง 2026-08-12** — `pipenv sync --dev` ไม่ติดตั้ง
มัน job `security` จึงไม่เคย audit และ Dependabot ก็ไม่เห็น แปลว่า **gunicorn
ซึ่งรับคำขอจริงทุกใบ ไม่มีใครเฝ้า CVE ให้เลย** · เจอตอนไปตรวจว่า Dependabot
ครอบอะไรบ้างแทนที่จะเชื่อว่าเปิดแล้วครบ (เป็นช่องเดียวกับที่ทำให้ด่าน license
ของ `[deploy]` ต้องข้ามบน CI — category นี้มองไม่เห็นจากเครื่องมือทุกตัวที่
ติดตั้งด้วย `pipenv sync --dev`)

## การปิด alert ของ CodeQL

**ทุกการ dismiss ต้องมีเหตุผลที่ชี้ไปหาหลักฐานที่ตรวจได้** — ชื่อเทสต์ · ข้อของ ADR ·
หรือคอมเมนต์ในโค้ด · หลักเดียวกับ `.zap/rules.tsv` และ `.gitleaksignore` ที่บังคับ
ว่าทุกบรรทัดต้องอธิบายตัวเองได้โดยไม่ต้องถามคนเขียน

**ข้อที่ขัดกับหลักของ [ADR 0037](adr/0037-where-logs-go-and-what-shouts.md) และยอมรับ
โดยรู้ตัว:** ตัว *กฎ* (job `codeql`) อยู่ในไฟล์ใน repo ตามหลัก แต่ *คำตัดสินว่า
alert ไหนไม่จริง* อยู่บน GitHub ไม่ได้อยู่ในไฟล์ — ต่างจากกฎที่คลิกไว้ใน UI ของ
Grafana ตรงที่มันมีผู้เขียน วันที่ และเหตุผลติดมาด้วย และถามผ่าน API ได้ แต่ก็ยัง
เป็นสถานะที่ไม่ผ่านการ review แบบ PR · มาตรการชดเชยคือแถวทบทวนทุก 6 เดือนในตาราง
ข้างบน เพราะ **ความเสี่ยงจริงของการ dismiss ไม่ใช่การตัดสินผิดวันนั้น แต่คือการที่
ไม่มีใครกลับมาอ่านอีกเลยเมื่อโค้ดรอบ ๆ เปลี่ยนไปแล้ว**

รอบแรก (2026-08-12): 6 alert · เป็นของจริง 2 (แก้แล้ว: `py/flask-debug` ที่ `run.py`
และการ log URL ของ store ดิบ ๆ) · false positive 4 · หลังแก้มี alert ใหม่อีก 1 ที่
`redacted()` เองเพราะ CodeQL ไม่รู้จักมันว่าเป็นตัวตัด taint — ปิดโดยอ้างเทสต์ที่
พิสูจน์ว่ารหัสผ่านไปไม่ถึง log

## กรอบเวลาแก้ช่องโหว่ของ dependency

นับจาก**วันที่รู้** (job `security` แดง หรือมีคนแจ้ง) ไม่ใช่วันที่ CVE ถูกเผยแพร่

| ระดับ | ต้องแก้ภายใน | ถ้าแก้ไม่ทัน |
|---|---|---|
| critical | **7 วัน** | ต้องปิดความสามารถที่เกี่ยวข้องหรือถอด plugin นั้นออกไปก่อน |
| high | **30 วัน** | บันทึกเหตุผลและวันที่คาดว่าจะแก้ไว้ในตารางส่วนที่ต้องมีคนลงมือ |
| medium | **90 วัน** | รวมเข้ากับรอบทบทวนรายไตรมาส |
| low | รอบทบทวนถัดไป | — |

**ระดับมาจากที่ไหน**: ใช้ CVSS ของตัว advisory เอง ไม่ตีความใหม่ ·
ถ้า advisory ไม่ให้คะแนนมา ให้ถือเป็น **high** จนกว่าจะมีคนอ่านแล้วตัดสินเป็นอย่างอื่น
พร้อมบันทึกเหตุผล — ค่าเริ่มต้นต้องเป็นฝั่งที่ปลอดภัยกว่า

**ไลบรารีของ plugin ต่างจาก core โดยตั้งใจ** ([ADR 0025](adr/0025-plug-points-and-supply-chain-isolation.md)):
ของ core ถอดไม่ได้จึงต้องแก้ ส่วนของ plugin มีทางออกที่เร็วกว่าการรอ patch เสมอ
คือ `DISABLED_PLUGINS` แล้วถอดไดเรกทอรีทิ้ง — กรอบเวลาข้างบนจึงเป็นเพดานของ
"เมื่อไหร่ต้องจบเรื่อง" ไม่ใช่ "เมื่อไหร่ต้องมี patch"

### ช่องโหว่ที่ประเมินแล้วว่ารับไว้ — เครื่องมือของ CI (2026-08-13)

การตรึง `pins/` ทำให้ dependency graph ของ GitHub โตจาก **126 เป็น 334 package**
— เครื่องมือของ CI เข้าระบบเฝ้าช่องโหว่**เป็นครั้งแรก** และสิ่งแรกที่มันบอกคือ
ของสี่อย่างที่อยู่ตรงนั้นมาตลอดโดยไม่มีใครเห็น (`Vulnerabilities` ของ Scorecard
ตกจาก 10 เหลือ 6) · **ไม่ใช่ของใหม่ที่การตรึงพามา — เป็นของเดิมที่เพิ่งมองเห็น**

| advisory | อยู่ที่ไหน | มี fix ไหม | คำตัดสิน |
|---|---|---|---|
| `GHSA-jmr9-qjv8-65gv` (CVE-2026-56876) — ของ `extract-zip` | dependency ของ `puppeteer` ใน `pins/pa11y/` | **ไม่มี** (กระทบทุกรุ่น · `npm audit` เสนอ `pa11y-ci@3.1.0` ซึ่งเป็นการ **ถอยรุ่นใหญ่** ไม่ใช่ fix) | **รับไว้** — ใช้ตอนแตกไฟล์ Chrome ที่ดาวน์โหลดจาก CDN ของ Google การโจมตีต้องมีคนยัด zip ที่แต่งไว้เข้ามาแทน |

**สามข้อของ `mcp` ถูกถอดออกแล้ว (2026-08-16)** — PYSEC-2026-3481/3482/3483
(CVE-2026-52870 / 52869 / 59950 — ล้วนเป็นเรื่องของการ*รัน MCP server* ซึ่งเรา
ไม่ได้ทำ) เคยรับไว้เพราะ semgrep 1.172.0 pin `mcp==1.23.3` ตายตัว · semgrep
1.173.0 ขยับเป็น `mcp==1.29.0` ซึ่งพ้นช่วงที่มีช่องโหว่ทั้งสาม จึง regenerate
`pins/semgrep/` แล้วถอด ID ออกจากรายการยกเว้นใน commit เดียวกัน
(ด่านสองทิศของ `scripts/audit_pins.py` บังคับให้สองอย่างนี้ไปด้วยกัน)

**ID ที่อยู่ใน backtick ในตารางนี้คือ ID ที่ `pip-audit` พิมพ์ออกมาจริง** และ
เป็นตัวเดียวกับที่อยู่ใน `pins/accepted-advisories.txt` ซึ่ง `scripts/audit_pins.py`
อ่าน — `tests/test_pins_audit.py` บังคับว่าสองที่นี้ต้องตรงกันทั้งสองทิศ
**ยกเว้นไว้ในไฟล์แต่ไม่มีเหตุผลในเอกสาร = แดง** และกลับกันก็เช่นกัน

**ด่านนี้บล็อกได้จริง** — job `security` แดงเมื่อเจอของใหม่ ทั้ง
ฝั่ง python (`pip-audit`) และฝั่ง node (`npm audit` ซึ่งอ่านจาก `package-lock.json`
โดยไม่ต้องมี `node_modules`) · `setup-node` อยู่ใน job นั้นด้วยเหตุผลนี้ และ
`tests/test_pins_audit.py` บังคับไว้ — runner มี npm ติดมาเองอยู่แล้ว ถอด
`setup-node` ออกแล้วสคริปต์จะยังทำงานได้ด้วย node รุ่นที่ไม่มีอะไรใน repo ประกาศไว้

**`npm audit` นับตาม package ที่ได้รับผลกระทบ ไม่ใช่ตาม advisory** — หัวข่าวคือ
"6 high severity vulnerabilities" แต่ต้นตอมีเรื่องเดียว (`extract-zip` ไหลไป
`@puppeteer/browsers` → `puppeteer` → `pa11y` → `pa11y-ci`) · รายการยกเว้นที่นับ
ตามหัวข่าวจะบวมตามจำนวน package ที่บังเอิญ depend กัน แทนที่จะตามจำนวนเรื่องที่
ต้องตัดสินใจ — และการตัดสินใจคือสิ่งที่รายการนี้บันทึก

**เงื่อนไขที่ทำให้คำตัดสินนี้หมดอายุ**: `extract-zip` มี fix เมื่อไหร่
Dependabot จะเปิด PR ให้เอง (เฝ้าอยู่แล้ว) — เงื่อนไขแบบเดียวกันของสามข้อ
`mcp` มาถึงแล้วจริงตามที่เขียนไว้ และคำตัดสินก็ถูกถอดตาม (ดูหมายเหตุข้างบน)

ข้อที่รับไว้**ไม่ได้อยู่ในของที่ deploy** — ไม่มีตัวไหนอยู่ใน `[packages]`,
`[deploy]` หรือใน image (ดู `Dockerfile`: ชั้น runtime ได้แค่ venv ของแอป)

## การรายงานช่องโหว่จากคนนอก

**เปิดรับแล้ว** — นโยบายอยู่ที่ [`SECURITY.md`](../SECURITY.md) ที่รากของ repo
ช่องทางเดียวคือ private vulnerability reporting ของ GitHub (ไม่มีอีเมลในไฟล์
โดยตั้งใจ) พร้อมกรอบเวลา: ตอบรับภายใน 7 วัน · ประเมินเบื้องต้นภายใน 14 วัน ·
ตัวแก้ตามตารางความรุนแรงข้างบน

**กรอบเวลาชุดเดียวกันอยู่สามที่** (ตารางข้างบน · ตารางอังกฤษและประโยคไทยใน
`SECURITY.md`) — `tests/test_security_policy.py` บังคับให้ทั้งสามตรงกัน เพราะ
คำสัญญาที่ให้คนนอกซึ่งไม่ตรงกับที่ทำจริง แย่กว่าการไม่สัญญาอะไรเลย
