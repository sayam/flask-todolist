"""แหล่งความลับเริ่มต้น: environment variable (ADR 0030)

**ตอบ `None` เสมอ** ไม่ใช่เพราะยังเขียนไม่เสร็จ — `app/secrets.py` ตกกลับไป
อ่าน environment ให้อยู่แล้วเมื่อแหล่งไม่มีค่านั้น การอ่านซ้ำที่นี่จึงเป็นการ
เขียนตรรกะเดียวกันสองที่ ซึ่งวันหนึ่งจะมีที่หนึ่งถูกแก้แล้วอีกที่ไม่ถูกแก้

มีไฟล์นี้อยู่เพราะ **ทุก scheme ต้องมีเจ้าของที่ประกาศตัวชัดเจน** ไม่งั้น
`env://` จะกลายเป็นค่าที่ `load()` ยอมรับเป็นพิเศษโดยไม่มีใครประกาศ ซึ่งเป็น
ข้อยกเว้นที่ซ่อนอยู่ในโค้ดแทนที่จะอยู่ในไดเรกทอรีให้เห็น
"""

from typing import Any


def connect(_url: str) -> None:
    """ไม่มีอะไรต้องเปิด — environment อยู่ที่นั่นอยู่แล้ว"""


def get(_handle: Any, _name: str) -> str | None:
    """ตอบ `None` เสมอโดยตั้งใจ — ดูเหตุผลที่หัวไฟล์"""
    return None
