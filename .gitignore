.venv/
__pycache__/
*.pyc
instance/
.env
*.db
.pytest_cache/

# messages.pot เป็นไฟล์กลางที่ regenerate ได้จากโค้ด ไม่ต้อง commit
messages.pot
sbom.json
.coverage
coverage.xml
.ruff_cache/
.mypy_cache/
htmlcov/

# ใบรับรอง TLS ของ dev (ออกด้วย scripts/dev_tls_cert.sh) — คีย์ไม่ควรอยู่ใน git
# แม้จะเป็นของ dev เพราะวันหนึ่งจะมีคนคัดลอกไปใช้ที่อื่นด้วยเหตุผลว่า "มันมีอยู่แล้ว"
deploy/tls/
.zap/out/
.gate-rounds.jsonl
secretdir/

# ผลวัด coverage ของ scripts/ — ขั้นตอนแยกใน job `test` เขียนไว้ให้ check_ratchets อ่าน
.cov-scripts.json
.coverage.scripts
