# อะไรที่ semgrep **ไม่** ต้องสแกน — ประกาศไว้เอง ไม่ใช้ค่าเริ่มต้นของมัน
#
# **ค่าเริ่มต้นในตัว semgrep ตัด `tests/` ทิ้ง** ซึ่งที่นี่คือ 61 จาก 136 ไฟล์
# — เกือบครึ่งของ python ทั้ง repo หายไปจากการสแกนโดยไม่มีบรรทัดไหนใน `ci.yml`
# บอก คนอ่านคำสั่งที่มี `--exclude migrations --exclude .venv` จึงเข้าใจว่านั่น
# คือขอบเขตทั้งหมด · **วัดแล้วว่ารวม `tests/` เข้ามาได้ 0 finding** การไม่สแกน
# จึงไม่ได้ประหยัดอะไร นอกจากทำให้ตัวเลขดูดีกว่าที่เป็น
#
# **ไฟล์นี้แทนค่าเริ่มต้นทั้งชุด ไม่ใช่เพิ่มจากมัน** — ของที่ต้องตัดจริง ๆ
# (`.venv/`, `node_modules/`) จึงต้องเขียนเองที่นี่ ลืมแล้วสแกนจะช้าลงมาก
# และเต็มไปด้วย finding ของโค้ดที่ไม่ใช่ของเรา
#
# `scripts/check_semgrep.py` อ่านไฟล์นี้เพื่อคำนวณว่า *ควร* สแกนกี่ไฟล์ แล้ว
# เทียบกับที่สแกนจริง — เติมบรรทัดที่นี่แล้วขอบเขตขยับ และตัวตรวจขยับตาม

# ไม่ใช่โค้ดของเรา
.venv/
node_modules/
.git/

# generate มา หรือไม่ใช่โค้ดที่รันตอนให้บริการ
migrations/
htmlcov/
.mypy_cache/
.ruff_cache/
.pytest_cache/
