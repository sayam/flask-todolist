# advisory ของเครื่องมือใน pins/ ที่ **ประเมินแล้วว่ารับไว้**
#
# `scripts/audit_pins.py` อ่านไฟล์นี้ แล้วบังคับสองทิศ ไม่ใช่ทิศเดียว:
#   - เจอ advisory ที่ไม่ได้อยู่ในไฟล์นี้ → **แดง**
#   - ID ในไฟล์นี้ที่ไม่โผล่ในผลอีกแล้ว → **แดงเหมือนกัน** (แปลว่ามี fix มาแล้ว
#     หรือ dependency ตัวนั้นหายไป — รายการยกเว้นที่ไม่มีใครถอดคือรายการที่
#     วันหนึ่งจะปิดของจริงโดยไม่มีใครรู้)
#
# **เหตุผลของแต่ละข้อไม่ได้อยู่ที่นี่** อยู่ใน `docs/SECURITY-CADENCE.md` หัวข้อ
# "ช่องโหว่ที่ประเมินแล้วว่ารับไว้" พร้อมเงื่อนไขที่ทำให้คำตัดสินหมดอายุ
# `tests/test_pins_audit.py` บังคับว่าทุก ID ที่นี่ต้องมีเหตุผลอยู่ในเอกสารนั้น
#
# ID ที่ใช้ต้องเป็นตัวที่ pip-audit พิมพ์ออกมา (PYSEC/GHSA) ไม่ใช่ CVE

GHSA-jmr9-qjv8-65gv
