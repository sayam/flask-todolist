# datasource ของ Grafana สำหรับ stack metrics — provision จากไฟล์เสมอ
# (ของที่คลิกไว้ใน UI หายไปพร้อม volume — หลักเดียวกับกฎของ Loki ใน ADR 0037)
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
