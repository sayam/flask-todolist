# CVE ใน OS layer ของ image ที่ **ประเมินแล้วว่ารับไว้** (ADR 0054)
#
# `scripts/audit_image.py` อ่านไฟล์นี้เทียบกับรายงาน trivy ของ job `image`
# แล้วบังคับสองทิศ ไม่ใช่ทิศเดียว:
#   - เจอ CVE ที่ไม่ได้อยู่ในไฟล์นี้ → **แดง**
#   - ID ในไฟล์นี้ที่ไม่โผล่ในผลอีกแล้ว → **แดงเหมือนกัน** (fix มาถึงแล้ว
#     ผ่าน digest ใหม่ หรือแพ็กเกจหายไป — รายการที่ไม่มีใครถอดคือรายการ
#     ที่วันหนึ่งจะปิดของจริงโดยไม่มีใครรู้)
#
# ขอบเขตของการสแกนอยู่ที่ step ใน job `image` ที่เดียว (HIGH/CRITICAL ·
# เฉพาะที่มี fix แล้ว — เหตุผลใน ADR 0054) — ไฟล์นี้จึงควรว่างเป็นปกติ:
# ของที่มี fix แล้วทางแก้คือขยับ digest (Dependabot ecosystem `docker`
# เปิด PR ให้เอง) ไม่ใช่การยกเว้น · ข้อยกเว้นมีไว้สำหรับวันที่ digest ใหม่
# ยังมาไม่ถึงแต่ต้อง ship ก่อน
#
# **เหตุผลของแต่ละข้อไม่ได้อยู่ที่นี่** อยู่ใน `docs/SECURITY-CADENCE.md`
# หัวข้อ "ช่องโหว่ใน OS layer ของ image" — `tests/test_image_audit.py`
# บังคับให้ไฟล์กับเอกสารตรงกันทั้งสองทิศ
#
# ID ที่ใช้ต้องเป็นตัวที่ trivy พิมพ์ออกมา (CVE-... หรือ DLA-.../DSA-...)

CVE-2026-14456
