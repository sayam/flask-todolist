# สำมะโนการถอดแกน governance ไป repo `verifiable-gates` — ADR 0075 ข้อ 6 · ขั้น 0
#
# **ไฟล์นี้เป็นคำตัดสิน ไม่ใช่สถานะ** — ทุกไฟล์ในขอบเขต (scripts/ · overlays/ · skill/ ·
# docs/comparison/ · ไฟล์ governance ที่ราก · เทสต์ที่เอ่ยถึงของพวกนั้น) ต้องถูกตัดสิน
# หนึ่งในสาม: `move` (ไปทั้งไฟล์) · `stay` (อยู่ทั้งไฟล์) · `split` (บางส่วนไป — บอกว่าอะไร
# ไปอะไรอยู่) · `tests/test_extraction_manifest.py` บังคับ partition สองทิศ: ไฟล์ที่ไม่ถูก
# ตัดสิน = แดง · รายการที่ชี้ไฟล์ผี = แดง (รูปเดียวกับ DATA-CLASSIFICATION และ gates.yaml)
#
# `stage` = ขั้นที่ย้ายตามแผน (2 แกน overlay · 3 ตัวตรวจ governance · 4 ตัวตรวจ supply chain
# · 5 เครื่องมือวิจัย) · เมื่อขั้นนั้นเสร็จ รายการ move ของขั้นนั้นต้องหายไปจากไฟล์นี้
# พร้อมไฟล์ (ไฟล์นี้หดลงเรื่อย ๆ จนเหลือแต่ stay/split แล้วถูกถอดในขั้น 6)
#
# **ตรวจ 2026-08-28: ข้อบนยังไม่จริงสำหรับขั้น 4 กับ 5** — ทั้งสองขั้นถูกประกาศว่าปิด
# (`e79e851` · `543ac3b` · `ef169b6` และ ADR 0075 ที่เขียนว่า "ที่นี่เหลือทะเบียนกับ
# adapter") แต่ยังมี **5 แถว `move` ที่ไฟล์อยู่ที่นี่เต็มตัว ไม่ใช่ adapter** และไม่มี
# module คู่ใน `vendor/verifiable-gates/src/verifiable_gates/` · แถวเหล่านั้นถูกเขียน
# ใหม่ให้บอกตรง ๆ ว่าค้าง แทนที่จะปล่อยให้อ่านเหมือนคำตัดสินที่ทำไปแล้ว —
# `tests/test_extraction_manifest.py` จับไม่ได้โดยนิยาม เพราะมันตรวจว่า "ทุกไฟล์ถูก
# ตัดสิน" กับ "ไม่ชี้ไฟล์ผี" ซึ่งแถว `move` ที่ยังไม่ทำผ่านทั้งสองทิศ
#
# **ตัดสินซ้ำ 2026-08-28 (เจ้าของ)**: ห้าตัวนั้นถูกอ่านทีละไฟล์ด้วยเกณฑ์เดียวกับที่ใช้กับ
# `whats_pending` ตอนขั้น 3d — *กลไกที่เหลือหลังหักของเฉพาะ repo ออก หนาพอที่ปลายทางอื่น
# จะได้อะไรไหม* ไม่ใช่แค่ "พกไปได้ไหม" · ผล: **3 กลับเป็น `stay`** (`build_vex` ·
# `build_eol_table` · `secret_scan_history.sh`) · **2 ยังเป็น `move`** เป็นคู่
# (`build_asvs_worksheet` + `build_gates_crosswalk` — parser ตาราง ASVS ซ้ำกันสองไฟล์และ
# vg ไม่รู้จัก worksheet เลย) · และการไล่ 17 แถว "adapter" ด้วย AST (def ไหนมอบงานให้ vg
# def ไหนตรรกะที่นี่ล้วน) พบว่า 14 บางจริง · 2 หนาเพราะทะเบียนของ todolist หนา (ถูกต้อง)
# · 1 (`audit_posture`) มีกลไกค้างที่คำอ้างเดิมไม่ได้พูดถึง — แถวนั้นถูกเขียนใหม่ให้ครบ
#
# วัดจริง 2026-08-25: scripts 33 ตัว import กันเอง 2 คู่ · CI เรียก scripts 14 จุด ·
# overlay ส่งออก 14 ไฟล์ · เทสต์ที่เอ่ยถึง scripts/overlays/skill 29 ไฟล์

version: 1
scope:
  - scripts/
  - overlays/
  - docs/comparison/
  - gates.yaml
  - scaffold.json
  - CLA.md

files:
  # ---------------------------------------------------------------- scripts — ของแอป (stay)
  - {path: scripts/a11y_fixture.py, decision: stay, reason: สร้างข้อมูลหน้าจอของแอปนี้ให้ pa11y}
  - {path: scripts/backup_drill.py, decision: stay, reason: ซ้อม backup/restore ของฐานข้อมูลของแอปนี้ (RUNBOOK-BACKUP)}
  - {path: scripts/bench_swap.sh, decision: stay, reason: bench การสลับ plugin ของแอปนี้ (ADR 0041)}
  - {path: scripts/build_password_blocklist.py, decision: stay, reason: generate ไฟล์ของแอป}
  - {path: scripts/check_tls_pfs.sh, decision: stay, reason: ตรวจ TLS ของ stack deploy ของแอปนี้}
  - {path: scripts/dast_scan.sh, decision: stay, reason: ZAP ใส่ stack ของแอปนี้ — login flow เฉพาะตัว}
  - {path: scripts/dev_tls_cert.sh, decision: stay, reason: ใบรับรอง dev ของ compose.tls}
  - {path: scripts/generate_openapi.py, decision: stay, reason: import app — สัญญา API ของแอป}
  - {path: scripts/generate_sun_table.py, decision: stay, reason: generate app/sun_data.py}
  - {path: scripts/install_purge_timer.sh, decision: stay, reason: systemd ของงาน purge ของแอป}
  - {path: scripts/loadtest_curve.sh, decision: stay, reason: k6 journey ของแอปนี้}
  - {path: scripts/n1_smoke.py, decision: stay, reason: import app — สัญญา N-1 ของ schema แอป}
  - {path: scripts/purge_cron.sh, decision: stay, reason: ตัวห่อ cron ของ flask purge-expired}

  # ---------------------------------------------------------------- scripts — แกน overlay (ขั้น 2)
  - {path: scripts/preflight.py, decision: stay, reason: adapter บาง ๆ เหนือ preflight ใน vendor (ADR 0077)}
  - {path: scripts/run_gates.py, decision: stay, reason: adapter บาง ๆ เหนือ harness ใน vendor (ADR 0077)}

  # ---------------------------------------------------------------- scripts — ตัวตรวจ governance (ขั้น 3 · หนึ่ง PR ต่อตัว)
  - {path: scripts/check_ratchets.py, decision: stay, reason: adapter บาง ๆ เหนือ `verifiable_gates.ratchets`+`measure` — ทะเบียน ratchet กับถ้อยคำไทยเป็นของที่นี่ (ขั้น 3c)}
  - {path: scripts/removals_census.py, decision: stay, reason: adapter เหนือ `verifiable_gates.removals` — `WATCHED` เป็นของที่นี่ (ขั้น 3d)}
  - {path: scripts/whats_pending.py, decision: stay, reason: อ่านหัวข้อภาษาไทยของเอกสารที่นี่ — กลไกที่พกไปได้บางเกินกว่าจะคุ้ม (ทบทวน 2026-08-26 · ขั้น 3d ย้ายแค่ removals_census กับ sync_counts)}
  - {path: scripts/sync_counts.py, decision: stay, reason: adapter เหนือ `verifiable_gates.advertised` — `TARGETS` กับตัวนับเป็นของที่นี่ (ขั้น 3d)}
  - {path: scripts/red_streak_census.py, decision: stay, reason: adapter เหนือ `verifiable_gates.red_streak_census` (ขั้น 3b)}
  - {path: scripts/rerun_census.py, decision: stay, reason: "adapter เหนือ `verifiable_gates.rerun_census` — ถ้อยคำไทยของรายงานเป็นของที่นี่ (ขั้น 5 · **ตัวสุดท้ายของแผนถอด** 2026-08-26)"}
  - {path: scripts/schedule_census.py, decision: stay, reason: adapter เหนือ `verifiable_gates.schedule_census` (ขั้น 3b)}
  - {path: scripts/audit_posture.py, decision: stay, reason: "adapter เหนือ `verifiable_gates.posture`+`check_names` — ทะเบียนธงกับถ้อยคำเป็นของที่นี่ (ขั้น 3e) · **ตรวจ 2026-08-28: คำว่า adapter ครอบแค่ครึ่งไฟล์** (541 บรรทัด · ตรรกะที่นี่ล้วน ~200) — สามก้อนที่ vg ยังไม่มี: (ก) `_gh_pages` ไล่หน้า API ซึ่ง**ซ้ำกับลูปใน vg `rerun_census._recent_runs` และ `red_streak_census._fetch`** → ควรยุบเป็น `gh.api_pages()` + `token_env` ที่ vg · (ข) `description_problems` **ซ้ำกับ `advertised.field_drift`** ที่ `sync_counts` ใช้อยู่ และทำให้ทะเบียน About มีสองใบ (`advertised_counts` vs `ABOUT_NUMBERS`) ที่ไม่มีอะไรบังคับให้ตรงกัน → ควรลบแล้วเรียกของ vg · (ค) badge/bestpractices ~50 บรรทัดผูกกับ project 14085 = ของที่นี่จริง · (ก)(ข) เป็นงานลบโค้ดซ้ำ ไม่ใช่ย้าย — บันทึกไว้ที่นี่จนกว่าจะทำ"}
  - {path: scripts/gh.py, decision: stay, reason: shim ที่ต่อ path ให้ vendor — ยังมีผู้ใช้ที่นี่ (`sync_counts`) (ขั้น 3b)}
  - {path: scripts/workflows.py, decision: stay, reason: shim ที่ต่อ path ให้ vendor — ยังมีผู้ใช้ที่นี่ (สองไฟล์เทสต์) (ขั้น 3b)}
  - {path: scripts/lint_commits.py, decision: stay, reason: adapter บาง ๆ เหนือตัวตัดสินใน vendor — hook `commit-msg` กับ job `commit-lint` เรียกพาธนี้ (ADR 0077 ขั้น 3a)}
  - {path: scripts/check_issue_handoff.py, decision: stay, reason: adapter บาง ๆ เหนือตัวตัดสินใน vendor — job `lint` เรียกพาธนี้ (ADR 0077 ขั้น 3a)}
  - {path: scripts/skeleton.py, decision: stay, reason: adapter เหนือ `verifiable_gates.skeleton` (ขั้น 3b)}

  # ---------------------------------------------------------------- scripts — ตัวตรวจ supply chain (ขั้น 4)
  - {path: scripts/audit_pins.py, decision: stay, reason: adapter เหนือ `verifiable_gates.advisories` — ไฟล์ล็อกกับถ้อยคำเป็นของที่นี่ (ขั้น 4)}
  - {path: scripts/audit_image.py, decision: stay, reason: adapter เหนือ `verifiable_gates.advisories` (ขั้น 4)}
  - {path: scripts/audit_plugin_deps.py, decision: stay, reason: adapter เหนือ `verifiable_gates.advisories` — สามทางที่ปลดได้เป็นของ ADR 0025 (ขั้น 4)}
  - {path: scripts/build_vex.py, decision: stay, reason: "**เปลี่ยนคำตัดสิน 2026-08-28** (เดิม move ขั้น 4 · ค้างมาตั้งแต่ `e79e851`) — อ่านโค้ดแล้วของ todolist ถอดไม่ออก: `REGISTERS` ผูกทะเบียน 3 แฟ้มกับสถานะ/เหตุผล OpenVEX ของแต่ละแฟ้ม · `PACKAGE` regex อ่านร้อยแก้วไทย `— ของ `pkg`` ใน SECURITY-CADENCE · `IMAGE_ACTION` พูดถึง base image ของเรา · `@id`/`tooling` เป็น URL ของ repo — เหตุผลเดิมเองก็บอกว่า 'ต้อง parametrize 6 จุด' ซึ่งคือย้ายซอง JSON ~30 บรรทัดแล้วทิ้ง config ~150 ไว้ที่นี่ · กลไกที่พกไปได้บางเกินกว่าจะคุ้ม (เกณฑ์เดียวกับ `whats_pending`)"}
  - {path: scripts/build_eol_table.py, decision: stay, reason: "**เปลี่ยนคำตัดสิน 2026-08-28** (เดิม move ขั้น 4) — พกได้ทั้งตัว (71 บรรทัด · เฉพาะ `PRODUCTS` กับ path ผูกปลายทาง) แต่กลไกที่เหลือคือ `fetch()`+`verify()` ~40 บรรทัดที่ไม่มีปลายทางที่สองต้องการ · 'ไม่รู้จักแอป' ตอบว่าพกได้ ไม่ได้ตอบว่าคุ้ม — เกณฑ์เดียวกับ `whats_pending` · กลับมาพิจารณาเมื่อมีโปรเจกต์ที่สองที่ตรึงตาราง EOL"}
  - {path: scripts/check_semgrep.py, decision: stay, reason: adapter เหนือ `verifiable_gates.scan_coverage` — ขอบเขตของ 'โค้ดของเรา' เป็นของที่นี่ (ขั้น 4)}
  - {path: scripts/secret_scan_history.sh, decision: stay, reason: "**เปลี่ยนคำตัดสิน 2026-08-28** (เดิม move ขั้น 4) — 48 บรรทัดที่เป็นข้อความไทยเกือบทั้งหมด (vg เป็นอังกฤษล้วน) ห่อ `gitleaks git --log-opts=--all` บรรทัดเดียว + กับดัก `$?` · **vg ไม่มีไฟล์ `.sh` เลยสักไฟล์** การย้ายคือเปิดภาษาที่สองให้ vg เพื่อหนึ่งบรรทัดที่พกได้ · เป็น runbook glue ของ SECURITY-CADENCE ที่นี่"}

  # ---------------------------------------------------------------- scripts — เครื่องมือวิจัย (ขั้น 5)
  - {path: scripts/asvs_probe.py, decision: stay, reason: adapter เหนือ `verifiable_gates.asvs_probe` (ขั้น 5)}
  - {path: scripts/measure_generated.py, decision: stay, reason: adapter เหนือ `verifiable_gates.measure_apps` (ขั้น 5)}
  - {path: scripts/build_asvs_worksheet.py, decision: move, stage: 5, reason: "**ยังไม่ย้าย** — สร้าง worksheet จากมาตรฐานที่ตรึง (todolist เก็บเฉพาะคำตัดสินใน `docs/ASVS.md`) · ขั้น 5 ปิดตัวอื่นไปเมื่อ 2026-08-26 (`543ac3b`) แต่ตัวนี้ไม่ได้ไปด้วย · ที่นี่ยังถือตัวเต็ม 179 บรรทัด และ vg มีแต่ `asvs_probe` ไม่มีตัวสร้าง worksheet — แถว `tests/test_asvs.py` ข้างล่างที่เขียนว่าจะเรียกจาก vendor หลังขั้น 5 จึงยังไม่จริง · **ค้างอยู่จริง ไม่ใช่คำตัดสินว่าไม่ย้าย**"}
  - {path: scripts/build_gates_crosswalk.py, decision: move, stage: 5, reason: "**ยังไม่ย้าย** — crosswalk gate↔ASVS generate เป็นของ registry ซึ่งอยู่ที่ vg แล้ว · ที่นี่ยังถือตัวเต็ม 143 บรรทัด และไม่มี module คู่ใน `vendor/verifiable-gates` · **ค้างอยู่จริง ไม่ใช่คำตัดสินว่าไม่ย้าย**"}

  # ---------------------------------------------------------------- overlays/ + skill/ (ขั้น 2 — ย้ายทั้งก้อน)

  # ---------------------------------------------------------------- docs/comparison/ (ขั้น 5)
  #
  # **คำตัดสิน 2026-08-25 คือ "ย้ายทั้งชุด" · 2026-08-26 เจ้าของแก้เป็นรูปที่ละเอียดกว่า**
  # ตัวที่ใช้งานอยู่ไป vg เป็นภาษาอังกฤษ · ต้นฉบับภาษาไทยเก็บเป็น reference ที่นั่น
  # · ที่นี่คงไว้เป็นบันทึกการพัฒนาที่ freeze แล้ว · และ `effort-log.csv` ไม่ย้าย
  # เพราะเป็นบันทึกของผู้วิจัย ไม่ใช่หลักฐานของ vg
  - {path: docs/comparison/README.md, decision: split, stage: 5, reason: ฉบับ active แปลเป็นอังกฤษไปอยู่ที่ vg · ที่นี่เหลือบันทึกการพัฒนาที่ freeze แล้ว พร้อมตารางคำตัดสิน (คำตัดสินเจ้าของ 2026-08-26)}
  - {path: docs/comparison/spec-notes-app.md, decision: split, stage: 5, reason: ฉบับอังกฤษไปอยู่ที่ vg · **ต้นฉบับที่ส่งให้ agent จริงเป็นภาษาไทย** เก็บที่ `vg/docs/comparison/reference/` และคงสำเนาไว้ที่นี่เป็นบันทึก}
  - {path: docs/comparison/results-2026-08-14.md, decision: split, stage: 5, reason: ฉบับอังกฤษไปอยู่ที่ vg · ต้นฉบับไทยอยู่ใน `reference/` ของ vg และคงสำเนาไว้ที่นี่เป็นบันทึก}
  - {path: docs/comparison/results-2026-08-14.json, decision: split, stage: 5, reason: ข้อมูลดิบถูกคัดลอกไป vg ให้อยู่กับรายงาน · คงไว้ที่นี่เป็นบันทึก}
  - {path: docs/comparison/effort-log.csv, decision: stay, reason: "**เปลี่ยนคำตัดสิน 2026-08-26**: เป็นบันทึกของ *ผู้วิจัย* ไม่ใช่หลักฐานของ vg · `tests/test_effort_log.py` ยังบังคับที่นี่ (ADR 0075)"}

  # ---------------------------------------------------------------- ไฟล์ governance ที่ราก
  - {path: gates.yaml, decision: split, stage: 6, reason: แถว baseline/business (94) เป็นทะเบียนตั้งต้นของ vg · แถว internal (21) + แถวที่พูดถึงแอปอยู่ที่นี่ — ถอดออกต้องผ่าน ADR ตามพื้น/เพดาน}
  - {path: scaffold.json, decision: stay, reason: config ของ instantiation นี้ — คือสิ่งที่ปลายทางทุกแห่งมีของตัวเอง}
  - {path: CLA.md, decision: stay, reason: CLA ของ repo นี้ — vg มีฉบับของตัวเองแล้ว}

  # ---------------------------------------------------------------- tests/ ที่เอ่ยถึงของในขอบเขต
  - {path: tests/conftest.py, decision: stay, reason: fixture ของแอป — เอ่ยถึง scripts แค่ในคอมเมนต์}
  - {path: tests/test_effort_log.py, decision: stay, reason: "**เปลี่ยนคำตัดสิน 2026-08-28** — เหตุผลเดิมคือ 'ตามไฟล์ข้อมูล' แต่ `effort-log.csv` ถูกเปลี่ยนเป็น `stay` ไปแล้วเมื่อ 2026-08-26 (บันทึกของผู้วิจัย ไม่ใช่หลักฐานของ vg) · เทสต์ย้ายออกจากไฟล์ที่มันอ่านไม่ได้ และสิ่งที่มันบังคับคือ ADR 0075 ซึ่งเป็นกติกาของที่นี่"}
  - {path: tests/test_checker_logic.py, decision: split, stage: 3, reason: unit test ของตัวตรวจแต่ละตัวไปกับตัวตรวจ · assertion ที่ผูกกับตัวเลข/ทะเบียนของ todolist อยู่}
  - {path: tests/test_script_roles.py, decision: split, stage: 3, reason: ทะเบียนบทบาทของ scripts — ตัวตรวจไป · แถวของ scripts ที่ stay 13 ตัวอยู่}
  - {path: tests/test_external_surface.py, decision: split, stage: 3, reason: logic ของ audit_posture ไป · ทะเบียนผิวนอกของ todolist อยู่}
  - {path: tests/test_pending_page.py, decision: split, stage: 3, reason: logic ของ whats_pending/sync_counts ไป · กองของ todolist อยู่}
  - {path: tests/test_pins_audit.py, decision: split, stage: 4, reason: logic สองทิศไป · ทะเบียน pins/accepted-advisories ของ todolist อยู่}
  - {path: tests/test_image_audit.py, decision: split, stage: 4, reason: logic ไป · ทะเบียน deploy/accepted-image-advisories อยู่}
  - {path: tests/test_semgrep_gate.py, decision: split, stage: 4, reason: logic ขอบเขตไป · ขอบเขตจริงของ todolist อยู่}
  - {path: tests/test_vex.py, decision: split, stage: 4, reason: logic OpenVEX ไป · เนื้อ vex ของ todolist อยู่}
  - {path: tests/test_gates.py, decision: stay, reason: ตรวจทะเบียนของ todolist สองทิศ — logic สากลถูกส่งออกเป็น scan_gates_registry อยู่แล้ว}
  - {path: tests/test_declared_prohibitions.py, decision: stay, reason: ข้อห้ามของ CLAUDE.md ของแอปนี้}
  - {path: tests/test_backup_drill.py, decision: stay, reason: ตามสคริปต์ที่ stay}
  - {path: tests/test_throwaway_database.py, decision: stay, reason: กันฐานจริงของแอป}
  - {path: tests/test_cadence.py, decision: stay, reason: ตารางรอบตรวจของ todolist — เรียก whats_pending จาก vendor หลังขั้น 3}
  - {path: tests/test_asvs.py, decision: stay, reason: คำตัดสิน ASVS ของแอป — เรียก build_asvs_worksheet จาก vendor หลังขั้น 5}
  - {path: tests/test_generated_tables.py, decision: stay, reason: generate ของแอป}
  - {path: tests/test_openapi.py, decision: stay, reason: สัญญา API ของแอป}
  - {path: tests/test_contributor_docs.py, decision: stay, reason: เอกสารของ repo นี้}
  - {path: tests/test_dependabot.py, decision: stay, reason: config ของ repo นี้ — เรียก lint_commits จาก vendor หลังขั้น 3}
  - {path: tests/test_exception_registers.py, decision: stay, reason: ทะเบียนยกเว้นของ repo นี้}
  - {path: tests/test_mode.py, decision: stay, reason: ของแอป}
  - {path: tests/test_plugins.py, decision: stay, reason: ของแอป — อ้าง check_ratchets แค่ในคอมเมนต์}
  - {path: tests/test_vendored_tooling.py, decision: stay, reason: ตรวจ *รอยต่อ* ระหว่าง repo นี้กับ vendor — เป็นของ repo นี้โดยนิยาม ปลายทางไม่มีรอยต่อนี้ (ADR 0077)}
